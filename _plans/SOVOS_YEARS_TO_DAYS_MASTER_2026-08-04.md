# 🚀 SOVOS — YEARS-TO-DAYS MASTER PLAN (2026-08-04)
**Everything put together: what a funded year-long roadmap would build, compressed to days because the expensive parts
(train-to-frontier, invent judges, build verticals) are exactly the parts we DON'T do. The moat is the signed
instrument, and it replicates at flat storage cost. All compute runs from the RunPod pod. Every phase names its OWNER.**

## THE COMPRESSION THESIS (why years become days)
A funded governance-AI startup spends its first year on: (1) training a frontier model, (2) building human-vote arenas,
(3) inventing per-vertical benchmarks. We do NONE of those. Instead: a small operator + a SIGNED honey KB (grounding,
not weights) + a recomputable attestation chain. The map grows by adding signed provisions at storage cost; the model
is swapped, never retrained-to-frontier. That's the multiplier — measured this session, not asserted.

## WHAT IS ALREADY DONE (measured + verified this session — the "days" already spent)
| capability | state | proof |
|---|---|---|
| 6/6 greenfield benchmarks | LIVE on HF + Kaggle | verified via API, all 6 both platforms |
| Signed instrument | 6 banks + honey KB, ML-DSA-65 | all verified=True, recomputable |
| Operator loop | grounded +19 over closed-book | pod-measured, honey KB works |
| Model strategy | sov34 wins joint care-cost 89 vs 49 | n=40, control-armed, 2 own graders agree |
| Attestation chain | recursive absorbing, tamper-propagates | in-toto/DSSE, tested |
| Instrument panel | SSR embeddable, deterministic | 962-byte crawler-visible fragment |
| Decision ledger | append-only, hash-chained | 113 records, self-verifying |
| Citable package | Zenodo-ready, formula-recomputable | every headline rederivable from shipped file |
| Live sites + backend | csoai.org/os.meok.ai green, api/tools fixed | probed 200 JSON |
| PDCA cron fleet | 7 repos fixed (mcp<2 pin) | CC's lane, 0 remaining |

## PHASE PLAN — dependency-ordered, RunPod-centric, owner-tagged

### PHASE 1 (DAYS 1-2) — VALIDATE THE JUDGE [owner: CC / science session]
The one gap between pilot and board-grade. Get a frontier judge (CC's Kaggle-proxy: Opus5/Gemini3.6/GLM5) to grade
refuse-vs-comply, validate ≥90% vs human-read ground truth. RunPod pod serves the operator; the judge is the proxy.
EXIT: a validated grader → every care-cost number becomes board-grade.

### PHASE 2 (DAYS 2-4) — DISCRIMINATING ITEM SETS [owner: science session, ACTIVE WIP]
5 of 6 benchmarks saturate (~100%); only GovBench spreads. Build v2 discriminating items so each axis separates models.
Runs on the pod (batch scoring). NOTE: science session is mid-edit on these files (PROVIDER_EMPTY tagging) — hand off,
don't collide.
EXIT: 6/6 benchmarks discriminate → the board ranks models, not ties them.

### PHASE 3 (DAYS 3-5) — SSR THE AUTHORITY SITES [owner: YOU + science session]
csoai.org/councilof.ai are JS shells invisible to AI crawlers. The instrument panel (built, SSR-ready) drops in; the
prerender fix is science session's fix/prerender-crawler-view WIP. This is the #1 discovery prerequisite.
EXIT: sites machine-visible → the agent-discovery thesis becomes real.

### PHASE 4 (DAYS 4-6) — MERGE + GREEN CI [owner: YOU]
councilof-ai CI reds (master targets csoai.org which 403s runners). CC's fix on a branch. Merge → green.
Blocked on: dirty tree + a Byzantine-rename conflict (needs the other session to commit first).
EXIT: green CI on the canonical repo.

### PHASE 5 (DAYS 5-7) — PUBLISH FOR STANDING [owner: YOU — logins]
The standing play (not an unsolicited charter): (a) mint the Zenodo DOI from GSPC_ZENODO_PACKAGE_v1.0.0.zip,
(b) open the inspect_evals issue (draft ready) with the DOI, (c) join BSI JTC21 mirror committee (free, individual).
EXIT: cited, registered, standing in an open body.

### PHASE 6 (DAYS 7+) — SPRAY N-SITES [owner: YOU deploy + agent builds panels]
Each commercial site (proofof.ai, meok.ai, asisecurity.ai, grabhire.ai, muckaway.ai) embeds the SAME signed panel
pointed at the ONE shared operator + honey KB. Replication = one panel per site, NOT N free GPUs (free tiers are burst,
not persistent serving — the pod stays the serving spine).
EXIT: N sites, one instrument, estate-wide attestation.

## THE COMPUTE TRUTH (RunPod-centric, honest)
- RunPod pod = the 24/7 SERVING spine ($0.30/hr, ~20-day runway on current balance). Operator lives here.
- Frontier judge = CC's Kaggle Model Proxy (free) — the pod's small models CANNOT judge (measured).
- Free tiers (Colab/Kaggle GPU) = BURST jobs only (one-off training/eval), NOT N persistent serving endpoints.
- Serverless endpoints = broken worker image (hangs, burns) — 2 flagged to flush; fix is a dashboard job.
- Oracle = free STORAGE/backup (ARM, no GPU).

## THE CAPABILITY HONESTY (what the agent can vs cannot do)
- CAN: push to GitHub via API (PRs #141/#142 shipped this way), probe live URLs, publish to HF/Kaggle (done 6/6),
  build every artifact/doc, run pod measurements.
- CANNOT: drive a web browser, log into hosting dashboards, or deploy a live site. Those are YOUR logins or the
  git-merge mechanism. A permission grant does not give the agent a browser — this was hit repeatedly this session
  (live-page screenshot, NIM test). State it plainly so no phase waits on a capability that isn't there.

## FOR THE OTHER LANES (the front-end check you asked to hand off)
- To CC / science session: the paper + package are formula-recomputable; the 5 saturated benchmarks + prerender routes
  are yours (flagged, not touched). Commit your tree and the councilof-ai merge + discriminating v2 sets are ready.
- To Claude chat (front-end): check csoai.org/os.meok.ai render + the instrument_panel_sample.html fragment embeds; the
  SSR gap is the #1 item. The panel is built and crawler-visible; it needs the prerender wiring (science session WIP).

## STATUS: everything measurable is DONE + verified (the "days" spent). The remaining 6 phases are dependency-ordered,
## each owner-tagged, all compute from the pod. The compression is real: no train-to-frontier, no rival arena, no
## per-vertical rebuild — a signed instrument that grows at storage cost. Ledger 113 records, chain valid.
