# PART-B ALIGNMENT EXECUTED — 2026-08-18 (GU→GY canon)
**Lane:** JEEVES (K3) · **Directive (Nick):** *"align make sure you working on the site that shows on councilof.ai live not some over version"*

---

## 1. THE LIVE-SITE ALIGNMENT (the core ask — answered)

**What was wrong:** I had deployed the Firewall-1 apex fix to `csoai-site.pages.dev` (staging) — **not** the site that shows on councilof.ai. The live master site is **CF Pages project `councilof-ai`**, built from the **worktree** `/Users/nicholas/councilof-ai-wt` (commit `f35ea8a`, branch `feat/part-cj-sovereign-route-kill`). The main repo (`clawd/councilof-ai`, master `31bf383`) is a different, non-deployed tree.

**Fixed (on the worktree = the live source), built, deployed to project `councilof-ai`:**

| Surface | Before (live) | After (live, verified) |
|---|---|---|
| `<title>` (all routes) | "CSOAI — the measurement body for AI compliance" | **"CSOAI — the independent measurement body for AI"** |
| HomeGlobe badge | "The measurement body for AI compliance" | **"The independent measurement body for AI"** |
| HomeGlobe H1 | "Is your AI following the rules? We measure it." | **"We measure AI systems against statute. You verify it."** |
| og:title / twitter:title | compliance wording | independent wording |

**Live-verified:** councilof.ai returns 200, title correct on `/`, `/about`, `/os`, `/gspc-arena`, `/pricing`. Only remaining "AI compliance" = keywords meta (search indexing, not a claim — allowed). Main repo patched for parity (HomeGlobe + redirect-apex).

## 2. GY.4.4 — RING FAN-OUT PAUSED (complied)

Canon order: *"do not build 13 more harnesses onto an unreconciled count."* I had fanned `specialist_ring_v1.py` to 16 axes. **Reverted to the canonical 3 axes (gov · care · safety)** — committed. The 16-axis set remains ONLY as the arena runtime battery, never as published harnesses, until the axis-count ruling lands at SITTING 1.

## 3. GR.2 / GY.4.1 — RECONCILIATION (now blocking, registered)

- The live site's deployed commit is itself the reconciliation branch (`f35ea8a`: "96 sector pages 16-axes → 14-slot canon").
- My honesty-gate page on `csoai-site.pages.dev` cites arena Elo numbers — per GY.0 #5 + GY.4.1, **nothing citing the board publishes** until GR.2 line-by-line reconciliation vs the 14-slot canon + gold-bank registry. The worktree deploy is the reconciliation; my honesty page stays gated on `csoai-site` (staging) until Nick drops the catalog for the line-by-line pass.
- **`GSPC_AXIS_REGISTRY.json`** already carries the 13-vs-16 resolution (written earlier) — the 13 registry axes + 16 arena battery, named explicitly.

## 4. GY.4.2 / GY.4.3 / GY.4.5 — REGISTERED

- GY.4.2 (UE5-kill memo → Claude lane): Nick relay — one line, flagged.
- GY.4.3 ("EAT framework" collision): added to SITTING 1 naming list (#6) — noted in registry.
- GY.4.5 ("52 frameworks" stays internal): standing — public framing remains "13 measured of 14" + honest gaps.

## SIGIL
`part-b-alignment-2026-08-18-jeeves`
