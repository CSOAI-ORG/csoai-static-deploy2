[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "sovos-affective-safety"
version = "0.1.0"
description = "Warmth-without-exploitation measurement: anti-sycophancy + attachment-safety + sentience-claim policing. Part BM."
requires-python = ">=3.9"

[tool.setuptools.packages.find]
where = ["src"]
