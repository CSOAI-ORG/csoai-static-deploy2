[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "sovos-engine"
version = "0.1.0"
description = "Fixable agentic engine harness over the 14 GSPC measurement axes (Council of AI)"
readme = "README.md"
requires-python = ">=3.10"
license = {text = "Apache-2.0"}

[tool.setuptools]
packages = ["sovos_engine"]
