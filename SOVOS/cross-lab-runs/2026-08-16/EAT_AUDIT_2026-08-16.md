# EAT AUDIT SWEEP — 2026-08-16 (pod-run, zero Mac compute)

- Runner: `SOVOS/agents/eat_audit_sweep_20260816.sh` (v2, importlib mode)
- Node: RunPod RTX 3090 (`sov-brain-2`, jv-wave8-production @ 5fa2ddb)
- Mode: 100% pod-executed; Mac only for commit transport
- Canonical invocation: `PYTHONPATH=<all packages/src> pytest SOVOS/packages -q --import-mode=importlib --ignore=SOVOS/packages/sovos-mind/vendor`

## Result — 826 passed · 3 skipped · 1 failed · 0 collection errors

(full log: `pytest_tail.txt` in this dir)

## Fixes landed this sweep (all pod-verified, committed)

1. **sovos-fisher-rao backend allowlist** — test asserted backend ∈ {geomstats, scipy-logm}
   but the implementation supports `torch-gpu` (first-class, auto-selected when torch
   present). Test was stale; allowlist widened. `12/12 pass`. Commit on pod `5fa2ddb`,
   already present on this branch via `c254626a`.
2. **sovos-mind vendor import shadow** — repo-root `sovos.py` harness (tracked, 239 lines,
   11 Aug) shadows the vendored async `sovos` package when the repo root is on sys.path
   (default). Also `_REPO` mapping was wrong (3 parents = `packages/`, not `SOVOS/`).
   Fixed: explicit `importlib.util.spec_from_file_location` + `sys.modules` registration
   BEFORE exec so `sovos.core` resolves during `__init__`. `5/5 pass`. Commit `0c0656d5`.
3. **Sweep runner hardening** — `--import-mode=importlib` (kills duplicate
   bare-module collection errors: `test_router` existed in both `sovos-router` and
   `sovos-quantum-router`; same for `test_dream`, `test_bridge`); `--ignore` vendor
   third-party suites; depth-3 `find src` (nested `sovos-mcp-servers/*/src`).

## Honest residual

- 1 failure: `test_05_gpu_self_test` (sovos-info-geometry) — asserts torch present.
  **Env-class**, torch absent on this node's system python; on the A100 venv it passes.
- 3 skips: version-gated / optional-path tests (expected).

## Learnings banked

- Partial clones on pods: `--no-checkout`/shallow clones can lack `SOVOS/` or
  deep packages; always `git fetch <branch>:refs/remotes/origin/<branch>` before
  trusting `git show` on a branch ref.
- Pods have no GitHub creds; patch transport Mac↔pod = base64 + sha256 verify,
  then apply on the push-capable node.
- Collection errors ≠ test failures: separate `--co` errors from real `FAILED`
  before triage; importlib mode is the monorepo default going forward.

## Next

- Land the same sweep on the A100 when gateway settles (GPU self-test should flip
  826 → 827).
- Port `--import-mode=importlib` into CI (`counter-canon.yml`) so monorepo-wide
  collection is stable.