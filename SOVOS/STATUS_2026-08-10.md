# SOVOS Monorepo — Current State (10 Aug 2026)

## ✅ What Actually Passes Today

These test files run cleanly on this Mac with `PYTHONPATH=src python3 tests/test_X.py`:

| Package | Test file | Tests | Status |
|---|---|---|---|
| **sovos-cpo-calculator** | `test_calculator.py` | 10 | ✅ NEW (committed 3e09c56) |
| **sovos-quantum-bridge** | `test_bridge.py` | 10 | ✅ passes (PennyLane sim path only — local install missing, pod passes) |
| **sovos-mind** | `test_mind.py` | 10 | ✅ passes |
| **sovos-jspace-hyperbolic** | `test_hyperbolic.py` | 10 | ✅ passes (Procrustes, Poincaré, Möbius) |
| **sovos-jspace-move** | `test_move_arithmetic.py` | 7 | ✅ passes (task-vector arithmetic) |

**Total: 47 tests passing across 5 packages on Mac.**

The 4 other packages (sovos-certification-loop, sovos-info-geometry, sovos-core, MCP servers) have additional tests; status varies.

## ❌ What's NOT Real (Yet)

1. **PennyLane on RTX 3090 GPU.** Locally, PennyLane is not installed (`import pennylane` fails on Mac). The "8.28ms/run" figure cited in the Aug 2026 strategic brief is from a prior pod session, not reproducible from this Mac today. RunPod pod may have it.
2. **UE Fire (visual world).** Zero `.uproject` files in the monorepo. This is months of work, not "one render loop."
3. **Real A2A protocol.** `Layer0Fabric` is a model with an `A2AAgent` dataclass. The Google A2A spec is not implemented.
4. **StateBus persistence.** Bus is in-memory only. Restarts wipe everything.
5. **Learned embeddings.** Water→Milk→Honey uses a deterministic hash vectorizer, not a trained model.
6. **Real CPO hardware.** The 9W/link is from the NVIDIA datasheet, not from a Broadcom switch on our rack.

## 📊 Brief Claims That Need Verification

The Aug 2026 strategic brief asserted:
- "107 tests passing across 8 packages" — **wrong**. Real count: 47 tests across 5 packages (this Mac).
- "Series A territory" — **premature**. No customers, no revenue, no team. The code is real but a company is more than code.
- "You are missing one render loop" — **wrong**. We are missing: persistence, real A2A spec, GPU validation, a real customer, and money.

## 🚀 What I Built Today (This Session, Honest)

**sovos-cpo-calculator** — Play 3 of the 90-day plan.

- 4 files: package + tests + CLI + README
- 10/10 tests pass
- Honest numbers from published datasheets
- README explicitly flags the model-vs-measurement gap
- Committed: `3e09c56`
- Branch: `jv-wave8-production`

This is a proposal-credibility artifact, not a deployed validation tool.

## 🎯 The Honest Bottom Line

The architecture is real. The numbers in the calculator are real. The brief's framing is half-right, half-flattery.

**What actually needs to happen:**
1. Verify PennyLane on the pod (GPU benchmark) — should run in next session if pod has budget
2. Ship one demo with a real customer (FishKeeper or similar) — this is the gate
3. Stop publishing briefs that over-claim. The code stands on its own.

— JEEVES, 10 Aug 2026
