# 🎯 ONE-SCREEN ACTION CARD — 2026-06-19

*Everything technical is GREEN. This card is the entire gap between "built" and "money moving." Generated by Claude/JEEVES after full multi-surface scan + 2-agent parallel audit.*

---

## 🔴 GATE 1 — Paste 4 env vars into Vercel (project `ui` = meok.ai), ~5 min
Without these, `/checkout` 500s and 95 staged emails can't fire. This is the SAME blocker as 16–17 Jun.

| Var | Value source | Unblocks |
|---|---|---|
| `STRIPE_SECRET_KEY` | Stripe dashboard → Developers → API keys (LIVE) | checkout, all 4 CSOAI price IDs already in Vercel |
| `RESEND_API_KEY` | Resend dashboard (mail.meok.ai already domain-verified) | outbound mail + 95 staged emails |
| `EMAIL_ADDRESS` | `nicholas@csoai.org` | mailer auth |
| `EMAIL_PASSWORD` | app password for above | mailer auth |

Then: **Stripe dashboard → flip to Live mode** + redeploy `ui`.

## 🔴 GATE 2 — Hit send on 5 outreach emails (drafts already exist), ~5 min
From `nicholas@csoai.org`. Angle = EU AI Act Art. 50 (2 Aug) + watermark (2 Dec) compliance via CSOAI cert + MEOK MCPs. Drafts: `clawd/revenue/PROSPECT_DRAFTS_2026-06-15.md` + `UK_PROSPECTS_2026-06-10.md`.

| # | Target | Hook |
|---|---|---|
| 1 | **Monzo** | fintech AI transparency / Art. 50 disclosure obligations |
| 2 | **Cera** | homecare AI — high-risk Annex III posture, ISO 42001 |
| 3 | **AccuRx** | NHS comms AI — clinical-safety + EU AI Act readiness |
| 4 | **Onfido** | identity/KYC AI — bias-detection + conformity assessment |
| 5 | **Faculty** | gov AI consultancy — white-label CSOAI cert partner |

## 🟡 GATE 3 — Distribution tokens (P1, when you have 5 min)
PyPI token (publish `agentaudit`) · npm 2FA (gaming pkgs) · `SMITHERY_API_KEY` · IndexNow keys on meok.ai/proofof.ai/csoai.org.

---

## 🤖 AUTONOMOUS FINDINGS (Claude can execute on your OK)

1. **Swarm backlog stalled** — SOV3 coordination bus: **169 tasks queued, 0 in progress, 2 completed.** Ralph executor isn't pulling the queue (known schema drift, `route.ts ↔ ralph_tasks`). The fleet is idle on a 169-deep backlog. → *Worth a dedicated fix session.*
2. **Typo-duplicate queen on VM** — `~/hive-staging/socialmediamananger-hive` (misspelled) sits alongside the correct `socialmediamanager-hive`. 32 stack.yml vs 28 canonical queens; the typo dir is dead weight. → *One-line `rm -rf` on VM after confirm.*
3. **CSGA severance 2/3 done** — `CSGA-GLOBAL-V33` + `CSGA-GLOBAL-V34` still active (SITE already archived). → *Archive both to finish severance (reversible).*
4. **GitHub dupes** — `awesome-mcp-servers` ×5, `cobol-bridge` ×3 are consolidation candidates (lower priority).
5. **clawd git drift** — untracked `coai/`, `haulage-deploy/`, `openmoe/` (new repos?) + 4 modified submodules. → *Need your intent before committing (don't want to embed nested repos by accident).*

---

## ✅ VERIFIED GREEN (no action)
SOV3 78.6% healthy · 6 NNs trained · VM meok-backend RUNNING (non-spot) · king :8077 up 2d · MEOK UI/API/product all 200 · meok.ai/csoai.org/openmoe.ai/proofof.ai all live · 556 GitHub repos · daily seals firing.
