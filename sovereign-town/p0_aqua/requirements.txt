# Sovereign Town p0_aqua — runtime + test dependencies
# Install: uv pip install -r requirements.txt

# Web server + HTTP/WebSocket stack
starlette>=0.40
uvicorn>=0.30
httpx>=0.27
websockets>=12.0

# Cryptography / signing
cryptography>=42.0

# MCP server SDK
mcp>=1.0

# Image generation for video packager
Pillow>=10.0

# Training / model pipeline (used by train_all_hives.py, flywheel_train.py)
numpy>=1.24
scikit-learn>=1.3
joblib>=1.3

# Data moat helpers
pandas>=2.0

# Browser-level tests (optional)
pytest>=8.0
pytest-playwright>=0.5
playwright>=1.40
