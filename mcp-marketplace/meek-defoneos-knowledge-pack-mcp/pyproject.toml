[build-system]
requires = ["hatchling"]

[project]
name = "meek_defoneos_knowledge_pack_mcp"
version = "1.0.0"
description = "MEEK DEFONEOS Knowledge Pack MCP — 3 whitepapers + 18 datasheets + datasets + licensing. MIT-licensed."
readme = "README.md"
requires-python = ">=3.10"
license = {text = "MIT"}
authors = [{name = "MEOK AI Labs", email = "nicholas@csoai.org"}]
keywords = ["mcp", "meek", "defoneos", "cold-email", "pricing", "seal", "uk-pilots", "knowledge-pack", "sovereign", "uk", "open-source"]
classifiers = ["Development Status :: 4 - Beta", "License :: OSI Approved :: MIT License", "Programming Language :: Python :: 3"]
dependencies = ["mcp>=1.0.0", "numpy>=1.24"]

[project.urls]
Homepage = "https://defoneos.com"
Repository = "https://github.com/CSOAI-ORG/meek-defoneos-knowledge-pack-mcp"

[project.scripts]
meek-defoneos-knowledge-pack-mcp = "meek_defoneos_knowledge_pack_mcp.server:main"

[tool.hatch.build.targets.wheel]
packages = ["meek_defoneos_knowledge_pack_mcp"]
