[build-system]
requires = ["hatchling"]

[project]
name = "meek_defoneos_cold_email_mcp"
version = "1.0.0"
description = "MEEK DEFONEOS Cold Email MCP — 12 cold emails ready to send to UK primes (Babcock + QinetiQ + BAE + Thales UK + Leonardo UK + DSTL + DAIC). MIT-licensed."
readme = "README.md"
requires-python = ">=3.10"
license = {text = "MIT"}
authors = [{name = "MEOK AI Labs", email = "nicholas@csoai.org"}]
keywords = ["mcp", "meek", "defoneos", "cold-email", "pricing", "seal", "uk-pilots", "knowledge-pack", "sovereign", "uk", "open-source"]
classifiers = ["Development Status :: 4 - Beta", "License :: OSI Approved :: MIT License", "Programming Language :: Python :: 3"]
dependencies = ["mcp>=1.0.0", "numpy>=1.24"]

[project.urls]
Homepage = "https://defoneos.com"
Repository = "https://github.com/CSOAI-ORG/meek-defoneos-cold-email-mcp"

[project.scripts]
meek-defoneos-cold-email-mcp = "meek_defoneos_cold_email_mcp.server:main"

[tool.hatch.build.targets.wheel]
packages = ["meek_defoneos_cold_email_mcp"]
