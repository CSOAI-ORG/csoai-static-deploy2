[build-system]
requires = ["hatchling"]

[project]
name = "meek_leanstral_mcp"
version = "1.0.0"
description = "Mistral Leanstral Lean 4 formal proof AI for mathematically proven-correct DEFONEOS-SEAL credentials. MIT-licensed."
readme = "README.md"
requires-python = ">=3.10"
license = {text = "MIT"}
authors = [{name = "MEOK AI Labs", email = "nicholas@csoai.org"}]
keywords = ["mcp", "meek", "defoneos", "project-aurum", "sovereign", "uk", "open-source", "mit-licensed"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: System Administrators",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
]
dependencies = ["mcp>=1.0.0", "numpy>=1.24"]

[project.urls]
Homepage = "https://meok.ai/os"
Repository = "https://github.com/CSOAI-ORG/meek-leanstral-mcp"

[project.scripts]
meek-leanstral-mcp = "meek_leanstral_mcp.server:main"

[tool.hatch.build.targets.wheel]
packages = ["meek_leanstral_mcp"]
