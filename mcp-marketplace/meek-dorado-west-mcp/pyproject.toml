[build-system]
requires = ["hatchling"]

[project]
name = "meek_dorado_west_mcp"
version = "1.0.0"
description = "MEEK DORADO West MCP — the EAST to WEST click-through with heavy ontology + AI governance. CRITICAL. MIT-licensed."
readme = "README.md"
requires-python = ">=3.10"
license = {text = "MIT"}
authors = [{name = "MEOK AI Labs", email = "nicholas@csoai.org"}]
keywords = ["mcp", "meek", "sov-space", "sovereign", "temple", "globe", "tui", "digital-twin", "uk", "open-source"]
classifiers = ["Development Status :: 4 - Beta", "License :: OSI Approved :: MIT License", "Programming Language :: Python :: 3"]
dependencies = ["mcp>=1.0.0", "numpy>=1.24"]

[project.urls]
Homepage = "https://csoai.org/sov-space"
Repository = "https://github.com/CSOAI-ORG/meek-dorado-west-mcp"

[project.scripts]
meek-dorado-west-mcp = "meek_dorado_west_mcp.server:main"

[tool.hatch.build.targets.wheel]
packages = ["meek_dorado_west_mcp"]
