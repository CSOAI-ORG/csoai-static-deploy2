# CSOAI csoai.org — redirect rules
# Static redirects placed before dynamic ones.
# Generated 2026-08-05 by JEEVES (K3 lane) to close the 13-route 404 audit.
# Cloudflare Pages applies these to static-asset responses only.

# 2026-08-12 — wholesale 301 to councilof.ai REMOVED (Kimi, AZ audit).
# The 2026-08-10 wildcard was a stopgap for the Vercel-1034 apex outage ("csoai is
# borken, just redirect it"). www.csoai.org is healthy again (verified serving packs),
# and the wildcard was an SEO/AEO hazard: it dropped deep paths (csoai.org/<pack> ->
# councilof.ai homepage), and councilof.ai answers every path with a meta-refresh
# shell — so 456 pack URLs were funnelling into an unrelated shell page.
# Restore directive with one commit if the consolidation is still wanted — but the
# correct form is /* -> https://councilof.ai/:splat 301 (path-preserving), and only
# once the packs actually exist on councilof.ai.
# Apex (csoai.org bare) still needs the Namecheap ALIAS record (Nick's manual item);
# the 1034 never reaches Pages, so no _redirects rule can fix apex from here.

# /about used to 301 to /index.html (my pre-C3 audit fix). Now /about has its own
# page. Remove the rule so the real /about.html serves.
# (NOTE: clean-trailing-slash is handled by the default Pages 308 to drop the .html)

# /arena and /gspc-arena must resolve to the real 12-greenfields page.
# Without these, Cloudflare Pages serves index.html for the bare path, which
# is the "4-axis GSPC stub hydration crash" observed 2026-08-01.
# The arena-hub page itself is fully static HTML — no JS to hydrate — so a
# correct route + the file = no crash.
/arena       /arena-hub.html  301
/gspc-arena  /arena-hub.html  301
/arena/      /arena-hub.html  301
/gspc-arena/ /arena-hub.html  301

# /gate is the refusal-gate demo page; Cloudflare Pages default clean-trailing-slash
# handles /gate/ automatically.
/gate        /gate.html       301
/gate/       /gate.html       301

# /prv and /gspc-prv resolve to the provenance axis page.
# gspc-prv.html is a REAL file. Cloudflare's clean URLs serve it at /gspc-prv
# and /prv natively (same as /master and /provbench). No _redirect rule is
# needed — and the previous 301-to-.html rule created an infinite loop
# (301 → .html → 308 back → 301). Removed 2026-08-08.

# sov-* -> council-* renames (2026-08-16 pod sweep)
/sov-zoom-microsecond.svg /council-zoom-microsecond.svg 200
/sov-infinite-zoom.html /council-infinite-zoom.html 200
/sov-fluid-viewer.html /council-fluid-viewer.html 200
/sov-globe-portal.html /council-globe-portal.html 200
/sov-local-viewer.html /council-local-viewer.html 200
/sov_space_visual.html /council-space-visual.html 200
/sov-time-canvas.html /council-time-canvas.html 200
/sov-sync-proof.html /council-sync-proof.html 200
/sov-three-eyes.html /council-three-eyes.html 200
/sov-time-canvas.svg /council-time-canvas.svg 200
/sov-zoom-second.svg /council-zoom-second.svg 200
/sov-5d-engine.html /council-5d-engine.html 200
/sov-space-vwm.html /council-space-vwm.html 200
/sov-suburb-3d.html /council-suburb-3d.html 200
/sov-zoom-hour.svg /council-zoom-hour.svg 200
/sov-zoom-year.svg /council-zoom-year.svg 200
/sov-city-3d.html /council-city-3d.html 200
/sov-zoom-day.svg /council-zoom-day.svg 200
/sov-portal.html /council-portal.html 200
/sov-bridge.js /council-bridge.js 200

# api sov-* -> council-* (2026-08-16)
/api/sov-arena /api/council-arena 200
/api/sov-crdt /api/council-crdt 200
/api/sov-openttd /api/council-openttd 200
/api/sov-town /api/council-town 200

# tier-2 codename file renames (2026-08-16)
/sov3-model-card.html /council-model-card.html 200
/sov3-system-card.html /council-system-card.html 200
/sov3-whitepaper.html /council-whitepaper.html 200
/sov7_synthesis_dashboard.html /council-synthesis-dashboard.html 200
/sov7_visual_synthesis.html /council-visual-synthesis.html 200
/sovos.html /council-os.html 200
/sovereign.html /council.html 200
/sovereign-os.html /council-os-page.html 200
/sovereign-2026.css /council-2026.css 200
/council-wiki /council-wiki 200
/SOV33_BFT33_COUNCIL.html /council-bft-council.html 200
/SOV33_PQC_SSM_Photonic_Speculative_Hypotheses_2026-07-30.txt /council-pqc-ssm-photonic-hypotheses-2026-07-30.txt 200
/blog-sovereign-fleet-gspc.html /blog-council-fleet-gspc.html 200
