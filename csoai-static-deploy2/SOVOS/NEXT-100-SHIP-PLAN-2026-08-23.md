# NEXT 100 MOVES — PRODUCTION SHIP + GROWTH (2026-08-23)

The measurement body is **live, signed, tested, on the infra**. These 100 moves ship it to production,
enhance the end-user, and compound growth — learned from the leaders (LMArena preference-Elo + OpenRouter
gateway) applied to OUR signed, independent, governance-measured moat.

## PHASE 1 · PRODUCTION SHIP (the last 20%) — 1–25
1. **Stripe** — wire `/book` → payment (the ONE revenue unlock; needs the key).
2. Your **production domain** — wire `/verify`, `/arena`, `/board` onto a `councilof.ai`/`csoai.org`
   subpath (the GHA merge / a clean project).
3. The **hardening** — e2e Playwright suite across all 13 surfaces + flows (regression).
4. The **load/security** — rate-limit `/api/*`, no key in the browser (done), CSP.
5. The **analytics** — lead + arena + verify counts.
6. The **CRM** — auto-route `/submit-lead` → CRM (buyer/insurer/regulator tags only).
7. The **email** — auto-follow-up on a booking lead (the "book a pack" → intro).
8. The **subscription** — the continuous re-measurement tier (€5–15k/yr) checkout.
9. The **API** — measurement-as-a-service endpoints (signed receipt API).
10. The **report builder** — export a signed, branded report.
11. The **multi-tenant** — buyer / insurer / regulator dashboards.
12. The **regulator dashboard** — the SB 315 audit-pack view.
13. The **insurer dashboard** — underwriting measurement.
14. The **honest-register linter** — CI gate, no over-claim.
15. The **redirects/canon** — the site canon checks (the DEPLOY-LOCK).
16. **Publish the spec + schema + I-D** (the standard) to the site's /specs.
17. The **accessible + responsive** pass (all end-user types, WCAG).
18. The **error/empty/loading** states on every surface.
19. The **AG-UI audio** events on the agent (the full wire).
20. The **subscription** pricing page live.
21. The **booking → payment → receipt** flow.
22. The **production** verification (live pubkey + index).
23. The **schema.org** JSON-LD on the surfaces.
24. The **sitemap/SEO** for the measurement-body land.
25. The **ship checklist** — CI green, all surfaces 200, signed, on the infra.

## PHASE 2 · END-USER ENHANCEMENT — 26–50
26. The **onboarding** — a first-interaction guided tour.
27. The **role-switch** — end user selects "regulator / insurer / deployer / investor."
28. The **personalized** land (per role).
29. The **search** — find an instrument / receipt / model.
30. The **compare** — side-by-side models (the gateway).
31. The **A/B judge** — the arena is the engagement loop (more duels).
32. The **streak/rewards** — incentivize judging (community data).
33. The **leaderboard** — the signed Elo (live).
34. The **share** — a signed receipt URL.
35. The **notifications** — watch a model / instrument.
36. The **localization** — EU/UK/US/VN.
37. The **report download** (PDF).
38. The **dark/light** toggle.
39. The **AG-UI chat** power-user (tools in chat).
40. The **keyboard** shortcuts.
41. The **mobile** responsive.
42. The **zero-knowledge** framing (consumer/regulator trust).
43. The **honest** copy everywhere (no over-claim).
44. The **feedback** loop (judge quality).
45. The **community** — a public arena.
46. The **bounty** — judge incentives.
47. The **dataset** download (the civic-good corpus).
48. The **certification-free** trust page.
49. The **case study** (Mosaic×Munich Re demo).
50. The **product** tour video.

## PHASE 3 · GROWTH (learned from the leaders) — 51–75
51. The **preference-Elo** published (the anti-LMArena, signed) — the differentiator.
52. The **governance-arena** grows (more models + domains).
53. The **pair-gap** (East↔West) as a second signal.
54. The **model catalog** — every row measured, not listed.
55. The **gateway** — instruments routes (Eunomia).
56. The **receipt-per-completion** (the proof on every surface).
57. The **base>finetune / RAG** findings — published + cited.
58. The **75-model fleet** measurement (quiet-GPU).
59. The **domain axes** (COBOL/insurance/bond) measured.
60. The **cross-model** preference (agents judging agents).
61. The **data licensing** — sell access, never the mint.
62. The **comparison** — us vs static leaders (the Y-axis).
63. The **leaderboard-governance** note (arXiv 2608.18117).
64. The **standards** — file the I-D, the schema, the spec.
65. The **methodology** — arXiv/DOI.
66. The **AG-UI / MCP** contributions (the wire + instruments).
67. The **insurer** + **finance** pitches (sent).
68. The **regulator** engagement (Art 85 / AI Office CoP).
69. The **community** — open the arena to models.
70. The **monthly digest** (industry leadership).
71. The **re-measure** vendors (never echo).
72. The **independence ledger** (continuous).
73. The **referee-capture** monitor (monthly).
74. The **civic-good** framing (measurement, not certification).
75. The **thesis-done** — the signed measurement body cited.

## PHASE 4 · SCALE + RESILIENCE — 76–100
76. The **multi-node** measurement (3090 + runpods).
77. The **autoscaling** harness.
78. The **reproducible** harness (one measurement).
79. The **statistical gates** (e-values, sequential, CI).
80. The **sequestered** runs (hiddens sets).
81. The **audit** (independent reviewers on the spec).
82. The **ethics advisor** (external eyes).
83. The **kill-switch** drill.
84. The **cold-restore** drill.
85. The **transparency** report (annual).
86. The **data-provenance** chain (hash-anchored).
87. The **backup** mirror (multi-region).
88. The **runway** model (data-licensing revenue line).
89. The **hire** line (auditor-shaped).
90. The **board** (C2PA/AIUC-1/OSSF standing).
91. The **legal** (dispensation clean; not certification).
92. The **compliance** (measurement, not cert).
93. The **playbook v2** triggers (Art 50 action, EN 18286 citation, detector release, VN CAB).
94. The **quarterly** whitespace re-verification.
95. The **90-day** gate self-check.
96. The **monthly** review.
97. The **killer** — the field standard adopted.
98. The **comparison** to LMArena/Vals (independent + signed).
99. The **annual** publication (transparency report).
100. The **ownership** re-verified — the measurement body is the Y-axis.

## The through-line
**Phase 1 steps 1–2 are the ship gate** — Stripe (the one key) + the production domain (councilof.ai).
Everything else compound. The **differentiator we ship + grow**: signed, independent, governance-measured
with a governance-preference arena (our LMArena) + a measurement-annotated gateway (our OpenRouter) — the
Y-axis, not the router, not the static leaderboard.

## Honest
Two account-level credentials unblock the ship: the **Stripe key** (revenue) + the **production-domain
deploy** (councilof.ai GHA). Everything else is buildable + buildable-on from here.
