# NEXT 33 MOVES — 2026-08-23 (execution plan)

Grounded in what's LIVE: insurer product page `/insurer`, verification `/verify`, 18 signed artifacts,
clean measurement, IP register, GTM products. These are the concrete next moves for revenue + build.

## PHASE A — Close the product + evidence (1–9)
1. Raise `eat_run_local.py` probe timeout 120s→200s (sov33-unified needs ~114s) → re-measure it clean.
2. Measure `sov33-unified` → sign → the 19th artifact (closes the model build).
3. Sign the `index.html` + `insurer.html` root (attest the landing, not just the verdicts).
4. Rebuild `evidence-index.json` to include insurer-page + pitch + (measurement) → re-sign.
5. Verify the whole set portable + in-browser.
6. Add a `/pricing` anchor the verify page links to (cross-link insurer↔verify).
7. `deploy` the updated evidence dir → live.
8. Update `ALL-LANE-BUILD-REGISTER` + `MASTER-STACK` with the revenue product.
9. Grammar audit the public pages (no certification claims) — pass.

## PHASE B — Sales / outreach execution (10–18)
10. Send the **Mosaic × Munich Re** pitch (drafted) with the live `/insurer` + `/verify` links.
11. Book the **5-day evidence pack** (insert contact, define the pilot use-case).
12. Populate the **CRM** (buyer/insurer/regulator tags only, zero "scored" rows).
13. Screen the deal against the **7-class COI taxonomy**.
14. Price the **per-receipt API tier** (entry) for the Art 50 marking limb.
15. Build the **insurer evidence pack** template (signed receipt + method + ledger + re-measure sample).
16. Prep the **SB 315 capability memo** (COI-screened, signed-artifact, buyer-side) for the 2028 audit wave.
17. Register the offer in the **compliance calendar** (Art 50 Dec 2 2026 marking cliff).
18. Reply/iteration loop after the first contact (measurement-not-prediction framing).

## PHASE C — Product hardening + measurement (19–27)
19. Implement the **sequential measurement protocol** as a persistent script (one model per load).
20. Push the **sov33-unified fix** decision: raise timeout (done) OR investigate the 114s load.
21. Investigate the ~114s sov33-unified load (slow disk? arena contention?) — optimize.
22. Rebuild `sov33-evolved` full prompt (faithful Modelfile) + `council-oowm` if needed.
23. Measure the remaining base models (qwen2.5:0.5b-instruct, deepseek-r1 w/ directive) clean.
24. Add the **refusal-tolerant directive** (EAT_DIRECTIVE) to the permanent protocol.
25. Run the **jail-probe** (Movement 5) with a real model → signed result.
26. Grow the **arena** (more rounds, more domains) → data moat.
27. Benchmark on the **75+ sovereign fleet** (Oracle micros) to quantify base-vs-finetune across the board.

## PHASE D — Data / IP / publish (28–33)
28. Register the **insurer product + verify system IP** in IP_REGISTRATION (append IP-A/IP-B already drafted).
29. Package the **measurement data as a sellable asset** (sell access, never the mint).
30. Publish the **methodology** (arXiv/DOI) — the clean-sequential + refusal-tolerant method.
31. Surface the insurer product + verify on **councilof.ai** (via the sanctioned GHA merge / redirect).
32. Wire the **data moat** into the 12-month runway model (data-licensing revenue line).
33. **Public launch post** — the "stranger-verifiable measurement body" hook + the insurer offer.

## Status
P1 (product+evidence) & P4 (data/IP/publish) are close; P2 (sales) is the near-term revenue engine;
P3 (hardening) is the compounding measurement moat. All honest, all grounded in live signed artifacts.
