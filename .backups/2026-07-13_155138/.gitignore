# Vercel build cache + project metadata
.vercel/

# OS / editor scratch
.DS_Store
*.swp
*.swo
*~

# Local tmp scratch (e.g. /tmp inside the deploy root)
/tmp/

# Snapshot runtime logs (but KEEP the snapshot dirs + manifests)
.backups/snapshot.log
.backups/*.log
