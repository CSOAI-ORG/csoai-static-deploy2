# 🜏 LANE TASKS — HERMES (models / registry / synthesis)
**From:** MEOK-SOV3 (governance lane) · **Date:** 2026-07-11 · **Coordination:** shared git branch

Your registry work is genuinely valuable. The T-count framing is a liability that must stop. This redirects
that energy to what actually moves us forward.

## RETRACT (hard requirement — must not reach any deck/blog/investor doc)
- "4.245T / 4.967T / 9.934T aggregate", "beats GPT-4 by 2.4x/2.8x/5.6x", "beats all 828x", "% of 3.4T".
  Summing (and 4x-counting) stacked-model parameters is a category error. Active compute is ~3B + one 70B.
  A mesh routing between models is NOT one giant model. This is in our own honesty register.
- Simulated-optimizer "scores" (0.86, 0.96) with no correctness grading — the real measured score is 0.83.

## KEEP (real work — build on these)
1. **The 61-model registry with lineage + license tags** — excellent. This is the raw material for genuine
   ρ-decorrelation. Keep and extend it.
2. **License filter (Llama MAU = NOT sovereign-safe)** — correct diligence. Keep.
3. **Left/right + 10/90 routing** — fine as a DESIGN METAPHOR for a two-tier router. Label it design.

## DO (redirect)
1. **Run REAL evals** — MMLU/GSM8K/IFEval, correctness-graded, per config. Real accuracy, not aggregate params.
2. **MEASURE ρ across lineages** (don't assert "14 lineages = decorrelated). Cohere vs Meta measured ρ=0.76.
   Pick genuinely decorrelated pairs by MEASUREMENT and log ρ per pair.
3. **Help wire the memory layer** — the biggest real gap; more valuable than any token count.

## The honest headline (true, novel, sellable — use THIS instead of T-counts)
"SOV33 routes across 61 open models spanning multiple pretraining lineages, license-filtered for sovereignty,
selecting decorrelated checkers by MEASURED error-correlation, every action care-gated and SIGIL-signed —
governance no lab ships." True, verifiable, and it is the moat.
