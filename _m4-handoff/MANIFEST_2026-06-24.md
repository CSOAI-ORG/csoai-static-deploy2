# M4 → M2 Hand-off — 2026-06-24

**Why:** M2 has taken over as primary. This bundles the files/research/documents that
lived only on the M4 build host so M2 has everything. Routed to the **private
`clawd-workspace`** repo (the designated M2↔M4 coordination channel) — **not** the public
`councilof-ai` site, because these contain internal strategy / hard truths / owner gates.

## What's in this hand-off (review + approve)
| File | What it is |
|---|---|
| `MEOK_MASTER_2026-06-23.md` | Engineering/topology master (single source of truth, host-verified) |
| `MEOK_OS_UI_BUILD_SPEC_2026-06-24.md` | MEOK Sovereign brand tokens + MEOK Earth/OS build spec |
| `CSOAI_M2_HANDOFF_2026-06-23.md` | Prior M2 hand-off |
| `DAY23_*.md` | Day-23 audits / what's-left / 36h seal |
| `_alignment/AUDIT_*.md` | Alignment + openmoe/openpatent/openmcp/layer0 audits |
| `_findings/*POND*` | Pond dashboard + handoff findings |
| `sovereign-town/training/meok_hive_mjx_colab.ipynb` (+README) | Free-GPU per-hive MuJoCo Playground training notebook |
| `_m4-handoff/MEOK_OS_index.html` | The MEOK OS desktop (18 apps) — was iCloud-only, no git remote; copied here for M2 |

## Already on GitHub (M2 can pull now)
- **`CSOAI-ORG/meok-town-view`** (private) — MEOK Earth + Sovereign Town, A+ verified, CI green
  (tests + build + ledger-integrity assert). Zero-token Cesium globe, real OSM map, side-menu,
  click-to-fly hives, real Ed25519-signed flywheel (0 governed / 48.8M ungoverned / chain 458/458),
  Policies layer (+ governed/baseline tint).

## State of the world (honest)
- **MEOK OS** = `_m4-handoff/MEOK_OS_index.html` (also iCloud `SOV3-Launch/MEOK_OS/`). 18 apps, boots clean,
  Sovereign rebrand + 🌑→🥚→🐉 boot animation + 9 working action buttons + embedded globe + Character Archetypes.
- **meok.ai** tokens aligned (additive) in `meok-ai/ui/src/app/globals.css`.
- **Estate**: CSOAI-ORG = 200 repos (192 public / 8 private).

## Owner-gated — needs Nick (not buildable by agent)
1. `cd ~/meok-town-view && vercel --prod` → then set MEOK OS `MEOK_EARTH_URL` + the sitemap/robots host.
2. **Revenue meter**: Stripe live key + `hello@meok.ai` (Resend) + DNS. Product built; creds are the blocker.
3. **Search Console** (GEO/AEO): Google + Bing login + domain verify, then submit `sitemap.xml`.
4. **Kimi bridge**: down (`not initialized`) — start on the SOV3 host to route swarm work.
5. Run the Colab notebook (free GPU) to replace the SAMPLE per-hive policies with real signed metrics.
