# DEFONEOS — per-route improvement audit (beat competitors · deepen SOV / AI-OS / SAP-SaaS)
**2026-07-02 · cop.html command router (~57 route families) · e2e: 57/57 execute, 0 errors, 0 console errors**

How to read: each row = a router family → what it does now → closest competitor → **the edge or gap** → **next lever** (🥊 beat-competitor · 🧬 SOV/AI-OS integration · 💳 SAP-SaaS productization). Grounded in the verified _compintel DB + a targeted CoT/TAK web check (2026). Honesty flags noted inline.

## The through-line (verified)
- **Nobody pairs a live COP with a signed, offline-verifiable receipt.** WorldMonitor = strong OSINT but *explicitly no symbology, no governance*. Palantir/Anduril = system-of-record, *no independent receipt*. Vanta/Drata = *checkbox theatre*, no runtime cover. Turing/CETaS names the exact gap: *"no authoritative, independent organisation … with the legitimacy to inspect and approve AI systems."*
- **CoT is the de-facto tactical standard** (ATAK/WinTAK/iTAK/TAK-Server, TLS+client-cert routing); 2026 = TAK edge-AI "sensor mesh" + service standardisation. Our signed CoT export is on the right rail → the win is *pushing signed tracks into TAK*, not just rendering them.
- **Governed-SaaS is green field**: no OSS white-label SaaS ships signed AI-governance baked in. Every route below can become a metered SAP-SaaS capability (x402 pay-per-proof) on the sovereign node.

---

## LIVE-FEED ROUTES (aircraft · marine · seismic · news · disasters · aq · space · sat · cameras · wildfire · aurora · radiation · weather radar · clouds · rail · comms · airports · power · pipelines · chokepoints · submarine cables)
- **Now:** free public feeds → globe entities with `_info` cards, feed-health dots, per-contact 🔏 sign / 🎯 → CoT, throttle-honest captions + auto-retry.
- **Closest competitor:** WorldMonitor / Flightradar / MarineTraffic (single-domain), Palantir Gotham (fused, closed).
- **Edge:** *one* governed globe fusing all domains, each contact signable + TAK-exportable; feeds labelled free/public/consented (privacy posture WorldMonitor can't match on cameras).
- **Next levers:**
  - 🥊 **Convergence → alerting**: today convergence is a readout; competitors sell *alerting*. Add a signed "watch box" (draw an area → sign it → notify when ≥2 streams converge inside). Real early-warning, signed.
  - 🧬 **Feed provenance in the ledger**: every layer load already can `_fhSet`; also emit a SIGIL per feed-pull so the *data lineage* (source, time, count) is itself signed — the audit story extends from actions to inputs.
  - 💳 **Per-feed SAP-SaaS**: each connector = a metered MCP on the sovereign node (customer binds their *own* AIS/ADS-B receiver → x402 per-query). The dome is the free shopfront; the node is the paid governed pipe.

## CoT / MIL-STD-2525 / TAK (cursor-on-target · export cot · import cot · military symbology)
- **Now:** render 2525 symbols, import CoT XML, export/copy/**download .cot** (new), per-contact → CoT, CoT tray.
- **Edge:** *signed* CoT — the export carries a receipt; no competitor signs tactical tracks.
- **Next levers:**
  - 🥊🧬 **TAK-Server push (sovereign node)**: browsers can't do TLS+client-cert to TAK Server. Architect the push on the SOV3 node: dome → signed CoT → node → TAK Server (mesh). This is the deepest defence integration and a genuine differentiator (signed + governed feed into ATAK). *Honesty: in-browser is export-only; the live push is a node capability, document it as such.*
  - 💳 **"Governed CoT relay" SAP-SaaS**: a metered service that signs + rate-governs + care-floors any CoT feed before it hits a TAK Server (Article-14 hard-stops on kinetic tasking). Nobody sells governance *on the CoT bus*.

## GOVERNANCE / MOAT (ai governance · rainbow security · bft 3d · threat poc · compare doctrines · sign all · export/print brief · sigil · verify)
- **Now:** 3D BFT spiral + SOV33 mind column, Rainbow-over-IP, threat-POC (see→detect→council→refuse→sign), batch-sign, signed brief (JSON/PDF/QR/link), verify.html round-trip w/ tamper detection.
- **Closest competitor:** Vanta/Drata (compliance theatre), Credo/Vijil (AI governance dashboards) — none *offline-verifiable*, none *runtime-refusing*.
- **Edge:** the care-floor *refuses* + the receipt *verifies with no server*. This is the whole thesis, and it's demonstrable.
- **Next levers:**
  - 🧬 **Live SOV3 council gate**: today the council vote in the POC is an honest *illustrative* model. Wire the dome's high-risk actions to the real SOV3 `/api/bft` gate on the node so a genuine 23/33 quorum ratifies before an action proceeds (falls back to illustrative when the node's offline — label it).
  - 💳 **"Signed System Card" SAP-SaaS** (the JSP 936 / Turing gap): the highest-value paid artifact. Any customer's AI system → governed assessment → signed, offline-verifiable System Card. This is the assurance product the market has no independent supplier for.
  - 🥊 **OSCAL export** of the governance state (NIST lingua-franca) so an auditor ingests it directly — the compliance-world analogue of CoT.

## SOV SPACE / SIM (sov space · response event · simulate · flood/medevac/etc)
- **Now:** talk-to-Sovereign + governed scenarios, multi-branch → BFT-feasible → PDCA, tool-burst on theatre.
- **Closest competitor:** Improbable/CAE sim (closed, heavy); nobody ties sim → governance → signed outcome.
- **Next levers:**
  - 🧬 **Real SOV3 sim bind**: route "run a scenario" to the node's sim MCP for a real model when available; keep the on-globe visual.
  - 💳 **"Governed digital-twin" SAP-SaaS**: customer scenario → signed, council-ratified outcome record — sellable to gov/defence planning + insurance.

## GODS-EYE / CYBER (cyber · protect the ai space · scan)
- **Now:** links to godseye.html (real external baseline scan → signed report).
- **Edge:** the scan output is *signed*; open-source stack, data stays in perimeter.
- **Next levers:** 🥊 broaden the OSS scan stack (nmap/Nuclei/ZAP/Prowler/Garak) on the node; 💳 metered "signed CISO self-scan" — the black-swan model (free OSS + free cert + flat sub + PAYG) already scoped.

## KNOWLEDGE / STATS / SITREP / BRIEF (research · population/gdp · what am i looking at · what have you learned · export/print brief)
- **Now:** Data-Commons stats, GDELT intel cards, sitrep, signed brief with feed_health provenance.
- **Next levers:** 🧬 route "research/what have you learned" to Hermes on the node (real learner) with a signed digest; 💳 the signed brief = a metered "situational report" product.

## OS-CONTROL + NAV + CUSTOMISE (arrange/close windows · dome/globe mode · grids · street view · space cam · scan area · near · waypoint · lite mode · no sound · resume · shortcuts)
- **Now:** all execute cleanly (e2e verified); new: session-resume, keyboard shortcuts, a11y.
- **Next levers:** 🥊 these are table-stakes done well; keep parity. 🧬 persist workspace layouts to the node per operator identity (DID) so a workspace follows the user across devices — ties to the AI-OS account model.

---

## PRIORITISED IMPLEMENTATION QUEUE (highest ROI first)
1. **Signed feed-provenance SIGIL** (every feed-pull signs its source/time/count) — extends the moat from actions to *inputs*; small, pure-win, in-browser. 🧬
2. **Watch-box convergence alerting** (draw → sign → notify on multi-stream convergence) — turns a readout into the *product* competitors sell. 🥊
3. **OSCAL governance export** (NIST) — the compliance-world CoT; unlocks auditor ingestion. 🥊🧬
4. **TAK-Server push architecture** (node-side signed CoT relay) — deepest defence integration; document node capability + dome-side "TAK endpoint" config. 🧬💳
5. **Signed System Card productization** (JSP 936 / Turing gap) — the flagship paid SAP-SaaS artifact. 💳

## Shipped this pass
- `.cot` file download (TAK-ready) on the CoT panel + tray — completes the export→TAK loop the search confirmed as the tactical standard.
- (prior) full sign/CoT/verify loop, feed-health, lite gating, session-resume, shortcuts, a11y.

## Honesty register
- Council votes / flywheel numbers in the POC are labelled illustrative unless bound to the live node — keep that label.
- TAK-Server *push* is a node capability, not in-browser — don't imply the browser talks to TAK directly.
- Competitor claims above trace to the verified _compintel DB; anything directional (market sizing) is marked as a lever, not a fact.
