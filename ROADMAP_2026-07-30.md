# What else is needed — execution map (2026-07-30)

Drawn from: Master Playbook EOD, Production Sweep, Greenfield Growth Honest, the Six Speculative
Hypotheses PDF, and the live state of this session. Ordered by *what I can actually action now* and
prioritised so the high-leverage moves don't wait on user/admin lanes.

Legend: 🟢 actionable this session · 🟡 needs a small lift · 🔵 needs user · ⚫ blocked (docker / GPU / vendor)

---

## TIER 0 — UNBLOCK (everything queues)

- [⚫ 0.1 DISK] Free headroom. 5.5 GiB free; 95% full. Candidates: csoai-static-deploy2 7.4G,
      sovereign-temple 4.2G, _TABS 2.4G. **User call.**
- [⚫ 0.2 COMMIT] 1,357 uncommitted files in clawd; git is coarse-protected. Needs a push lane.
- [⚫ 0.3 CREDENTIALS] NVIDIA key into the secret store. User has it; needs to be put where the code
      can read it. Proves itself via a real 401 today.
- [⚫ 0.4 DOCKER] `brew reinstall --cask orbstack` then launch OrbStack.app once. Without this, the
      7-service ai-hub stack stays down. **This is the single biggest blocked item this session.**

## TIER 1 — BACKEND HARDENING (runnable without GPU, no Docker)

- [🟢 1.1.flywheel_daily_drift] Add `--models` support to the daily run so the cron uses the
      just-promoted keystone trio: `python3 flywheel.py --models clan-sovereignty-cited,clan-sovereignty-refusing`.
      Wrapper already wires `--daily`; verify it threads. (ETA 5 min.)
- [🟢 1.1.survival_e2e] Wrap `survival_matrix.run_matrix()` into a Fastify `/survival/run` endpoint
      on the existing `flywheel/src/server.ts` wrapper. Single endpoint, ~15 lines, real output.
      Same shape as `/board`. (ETA 10 min.)
- [🟢 1.1.equivalence_e2e] Wrap `equivalence.measure_divergence()` into `/ec/measure` on the wrapper.
      Takes `{ec: signed_dict, behaviour_result: dict}` → returns divergence report. (ETA 15 min.)
- [🟡 1.2.sibling_lane_recover] The 9 modules + 3 JSONs from the CC handoff that aren't on this disk.
      Open a question to user re: which workspace they live in (`~/clawd/csoai-…`?). Don't guess.
- [🟡 1.3.CELLAR_backoff] EU anchor (CELLAR) still fail-closed UNREACHABLE. Needs fresh-IP deploy to
      verify daily-cron design. **Requires 0.4 docker** + a network reachability check.
- [🟢 1.4.lens_seed_grow] conduct=5 provisions, continuity=8 items, survival=5 cells in this session's
      sample. Each is "a seed, not coverage" per the spec. Add 2 small batches (10 more provisions
      in each) → annotate as `seed v2`. Deterministic, no LLM. (ETA 20 min.)
- [🟢 1.5.triage_446_unwired] The single biggest production win per the Production Sweep. Run a
      Python script that walks all `*.py` in `~/clawd/csoai-static-deploy2/`, checks each for
      `[MODELED]` / `[RUNNING]` / `[BUILT]` tags + whether it's imported by ≥1 other file, and
      writes a triage manifest `benchmark-results/triage_2026-07-30.json` with KEEP/ARCHIVE/DELETE
      buckets. **User owns the DELETE bucket.** (ETA 30 min.)
- [🟢 1.6.PROVBENCH_BUMP] Head is `5bf5d28` with the canonical CI (n=20 → 15% rule-of-three).
      Take the opportunity to add the COSE ML-DSA-65 signed-as-binding = 4/4 survival cell into
      `survival_matrix.py` (mirror C2PA-v2.4) → strengthens the SURVIVABLE/PQC-READY argument
      from the PDF. (ETA 20 min.)
- [🟢 1.7.diversity_e2e] Wire `n_eff` calculation against the sovereign roster. The PDF says
      distilled-SSM-leg is the diversity fix; until that's built, measure current diversity. Output:
      a JSON of pairwise ρ across all 21 day-1 subjects. (ETA 10 min, ~21×21/2 small queries.)
- [🟢 1.8.run_provbench_paper] Once CI hits the canonical bound, draft the arXiv paper's table
      using only measured cells (the user has the boilerplate). **Don't write text**, structure the
      table from `provbench-canonical-bound.json`.

## TIER 2 — FRONT END (needs TIER 0.2 commit to ship)

- [🟡 2.1 /ledger top-level nav] The refutation ledger IS the moat and currently invisible.
      Master site = `~/clawd/councilof-ai`; needs an `<NavBar />` edit. **Owner lane = CC.**
- [🟡 2.2 Live anchored-to panel] Code exists, page doesn't consume it. Wire it. **Owner = CC.**
- [🟡 2.3 /verify relabelled] "chain intact" = tamper-evidence, NOT authenticity. One-line change.
- [🟡 2.4 "What we don't claim" panel] Cheapest credibility signal. One section. (Owner = this lane
      for copy, CC for placement.)

## TIER 3 — GREENFIELDS (status, target all 5 LIVE)

| Axis        | State                            | Next                                                    |
| ----------- | -------------------------------- | ------------------------------------------------------- |
| Governance  | ✅ LIVE (UK anchor)               | Expand CareGate coverage of Art 5 prohibited practices   |
| Safety      | ✅ LENS LIVE (conduct × 5)        | Add 8 more provisions to seed (1.4 above)               |
| Provenance  | ✅ MEASURED + PUBLIC (0/20, 13.9%)| Push 15-asset re-run after PQC binding cell added (1.6)  |
| Continuity  | ✅ LENS LIVE + SIGIL v2 4/5       | Measure ML-DSA-65 signed chain explicitly               |
| Care_cost   | ✅ MEASURED this session          | **Already best — find_besT will land**                  |

- [🟢 3.1.care_cost_publish] Once `find_besT_2026-07-30.json` lands, the joint care_cost winner IS
      the strongest care claim to publish. Move it into `production_ready.json` shape (already in
      the repo's working tree) so it's signed and queryable.

## TIER 4 — PUBLISH

- [🔵 4.1 ProvBench paper → arXiv] Drafted, numerically corrected, prior art verified. One
      session of writing.
- [🔵 4.2 Announce (DSIT/RTAU outreach)] `ai-assurance@dsit.gov.uk`. Position NPL-complementary.
- [🔵 4.3 The 90-day revenue path] Fixed-scope Art 50 / provenance-survival audit, signed evidence
      pack, £5–15k. Need ONE paying customer for proof.

## TIER 5 — PRODUCTION-DEFECT WATCH (mindset walk on every change)

- [🟢 5.1 cwd-independence audit] The Greenfield Growth doc found 7 modules with bare-relative store
      paths (DecisionLedger + 6 others). The fix script in `gspc_inspect` needs to be ported to
      run against the new `find_besT.py` outputs (don't write the same store under a relative path).
- [🟢 5.2 BLOCKED != FAIL] Three-outcome pattern (acquire / flag-locked / not-yet-derivable). The
      EC engine already does this; a unit test that proves the three states would harden it.

## ACTIVE MACHINES (PIDs)

| PID    | What                                       | ETA          |
| ------ | ------------------------------------------ | ------------ |
| 78702  | EAT stack (sov-sovereign-v4, step 2/3 now) | ~45 min      |
| 49549  | find_besT day-1 (model 3/21 in flight)     | ~50 min      |

When both boards land: post the BEST model + composite here. → That becomes the input to
TIER 3.1 (care_cost_publish).

## THE NEXT MORNING I CAN ACTION WITHOUT USER INPUT

In rough order of cost / benefit (smallest first):

1. **survival_matrix binding-extension** (1.6, 20 min) — adds PQC binding as a real cell, not a doc.
2. **find_besT board read + care_cost_publish** (3.1, 5 min once board lands).
3. **flywheel wrapper endpoints** (1.1, 25 min) — turns the keystone trio into a real `POST /survival/run`
   and `POST /ec/measure` so the dashboard can drive them.
4. **triage_446_unwired** (1.5, 30 min) — bigger. Single biggest production win. Depends on having
   user consent for the DELETE bucket.
5. **provbench-canonical-bound table** (1.8, 60 min) — gives the arXiv draft something real to point at.
6. **n_eff diversity scan** (1.7, 10 min once tooling exists) — inputs to the "distil SSM leg" decision.
