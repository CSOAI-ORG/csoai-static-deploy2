# 🐉 MASTER PLAN — aligned across all lanes, phased (2026-07-14)
_One phased plan over Fable (front-end + verify + research), Hermes (brains + SSD-stream + outreach), Claude
Science (sim + proofs), MEOK Labs (hardware), owner (Nick). Honest register: ✅ DONE · 🔓 OWNER-GATED · 🔬 LEAD.
Supersedes REST_TO_DO_ALIGNED for sequencing._

## The spine (one sentence)
**Adopt the best open base + inference + merge tooling; own the governance no base has** — care-gated-BFT +
SIGIL seam + signed memory + OSCAL — proven on CPU, verified with real libraries/models, running today on 16GB.

## PHASE 0 — Foundations (✅ DONE, measured + committed this session)
- Fluid pyramid (12@0.5 vindicated) · 4-brain (+48%) · A–P governed pipeline · mirror-auditor (0.434, escalate-to-frontier)
  · nesting law · brain-merge laws (route/distill, fake caught) · Venturi seam (tamper-caught) · governed-robustness **#1 board**.
- **112→ ~100 caps, 0 broken, SHIP-READY** (shared readiness file; lanes converging).
- Real models named + licensed (Qwen3 Apache, DeepSeek MIT); open-source landscape scraped → every law maps to a real library.

## PHASE 1 — The governed local sovereign (✅ DONE + RUNNING on 16GB)
- `sov33_local_sovereign.py` / `meok_sov33.local`: Ollama Qwen3 → care-gate (fail-closed) → Ed25519 SIGIL emit;
  **route** (small→large) + 4-brain **council** + OpenAI-compatible **HTTP server**. Live-tested end-to-end.
- **meok-sov33 v0.2.0** built + twine-check PASSED (bundles the local sovereign).
- **os.meok.ai dock wired** to the local endpoint (opt-in `localStorage.meok_local_sovereign`, cloud fallback).

## PHASE 2 — Ship the artifacts (🔓 OWNER-GATED — secrets/keys only)
| step | who | gate |
|---|---|---|
| `twine upload` meok-sov33 v0.2.0 | Nick | PyPI token |
| Deploy the dock change to os.meok.ai | Fable (vercel CLI) or Nick | — (Fable can fire) |
| Stripe Test→Live + pricing "ratify" | Nick | Stripe keys |
| DNS on broken domains | Nick | Vercel domains |

## PHASE 3 — The scale numbers (🔓 OWNER-HARDWARE — the only real blockers left)
1. **Kaggle GPU capability grade** — notebook `notebooke3e821442d` (GPU+internet ON); one paste of the combined cell → GSM8K + robustness #1. *(Browser broken this session → Nick's paste.)*
2. **48GB-Mac / cloud daily-driver** — DeepSeek-V4-Flash via `ssd-moe/deepseek-v4-flash-mlx` or MoE-Infinity → the real ~40 tok/s. *(16GB Mac can't run 158–284B; needs bigger hardware.)*
3. **Colibri/GLM-5.2 run-book** — needs ~370GB NVMe + Metal; **neither sandbox nor 16GB Mac runs it** — bigger machine/cloud only.

## PHASE 4 — Adopt the real libraries (next build, non-gated where hardware allows)
- **MergeKit + Mergenetic** → real 4→1 brain (soup same-base Qwen3 experts, evolutionary recipe search).
- **MoE-Infinity / FlashMoE** → turn Hermes' 64× compute-avoided proxy into wall-clock (on a GPU/48GB box).
- **HarmBench / StrongREJECT / BeaverTails / JailbreakBench** → run the care-gate → publishable *standard* safety number.

## PHASE 5 — MEOK Labs (hardware, aligned)
- Assurance Radar (Ed25519 frames, OSCAL) = the physical signed substrate. Reuse `governed-robustness` care-gated-BFT
  as multi-frame sensor fusion (drop spoofed frames, quorum, signed emit). Open humanoid bodies = adopt, not built.

## Lane split (steady state)
| lane | owns | now |
|---|---|---|
| **Fable** | front-end, verify, research, local sovereign | dock wired; can deploy; keep adopting libraries |
| **Hermes** | SSD-stream, OWEM training, outreach | swap proxy→MoE-Infinity on real HW; honest 6-lever held |
| **Claude Science** | sims, proofs | reconciled; fix the V4-Pro 1.6T→862B (base vs deployed) figure |
| **MEOK Labs** | hardware | radar live; wire care-gated-BFT sensor fusion |
| **Owner** | secrets + big hardware | PhaseΩ 2–3: PyPI/Stripe/DNS + Kaggle paste + 48GB box |

## The honest headline
**Production-ready + measured** (E2E 100/100, governance reproducible, red-team 40/40, robustness #1, capability
0.71 deployed, a governed local sovereign running on 16GB). **Owner-gated only:** publish secrets + two hardware
numbers (Kaggle tok/s, 48GB daily-driver). Moat intact: adopt everything open; own the governance.
