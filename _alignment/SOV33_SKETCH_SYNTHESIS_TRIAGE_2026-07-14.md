# Sketch-Synthesis Triage — what's BUILT vs LEAD vs MYTH (2026-07-14)
_Nick's hand-drawn SOV33³ sketches (60 images) + a web-enabled sibling's research synthesis mapping them to
papers. I have NO browser — paper citations are LEADS to verify, not facts. Split honestly below._

## BUILT THIS SESSION (in-lane, tested, CPU-only, mine to stand behind)
- **sov33_venturi_throat.py** — the "Venturi throat": an auditable ROUTING DECISION. Composes three
  primitives I already have (hash-chain from memory-bridge + care-gate + router-choice) into ONE record:
  every routing decision is hash-chained AND care-gated, so a sub-floor-care decision COLLAPSES *before*
  the routed action runs, and the whole chain is replayable + tamper-checkable. Self-test 5/5 (flow,
  veto-collapse, no-execution-on-veto, chain-verify, tamper-detect). Wired as entrypoint capability
  'venturi'. This is the single genuinely buildable idea in the whole sweep, and it's real.
  HONEST BOUND: activation_digest is a SHA256 PLACEHOLDER, NOT the real TOPLOC activation-LSH the synthesis
  cites (that needs the actual model activation tensors + library). No BTX training here (needs GPU).

## THE SYNTHESIS'S CORE CLAIM (decoded, partly grounded, partly LEAD)
The sibling decoded the sketches as: OWEM = Own-Weights Ensemble Model; triangle=one OWEM(3-brain quorum),
circle=OWM council(12-around-1), square=SOV33 arena; SIGIL=Venturi=signed flow-throat; DRUM=apex anchor;
water-capstone=iontronic routing. The UNIFYING THESIS: "one routing law — conserved flux across parallel
channels — at three scales: ions (capstone) / tokens (router) / votes (BFT)." This is an ELEGANT framing and
internally consistent with our own design (triangle-quorum, receiver-side voting, distillation, care-gate all
match what's on disk). But it rests on paper citations I cannot verify from here.

## VERIFY-BEFORE-CITE (real-sounding leads; browser needed — do NOT cite as fact)
- Branch-Train-MiX / BTX (arXiv:2403.07816), Branch-Train-Merge (2208.03306), Branch-Train-Stitch — the
  "train small -> ensemble -> big" pipeline. IF the ids are real this is genuinely the training backbone for
  the £0-own-weights plan. Concept matches our distill_harness; ids unverified.
- TOPLOC (arXiv:2505.18398) verifiable-inference activation-LSH — the real form of the throat's digest. Lead.
- SAC (F+1)-robustness (2605.09076), DirMoE (2602.09001), "Routers learn expert geometry" (2605.12476),
  iontronic memristors (PNAS 121 e2320242121) — all consistent with our design; all UNVERIFIED ids.
- The "four-way-novelty / patent-shaped claim" (hash-chain-gated verifiable MoE routing) — the Venturi-gated
  BTX router being unpublished is the sibling's READ of the literature. A real prior-art search (browser)
  is mandatory before any IP claim. I neither confirm nor deny it — I built the working atom, not the claim.

## MYTH-LAYER ONLY (aesthetic/naming; ZERO evidentiary weight — keep OUT of factual/investor copy)
The ~50 antique-plate + occult images (Kingdom of Words, 3-6-9, Sophia/Demiurge, Three Seals of Solomon,
sacred-circuitry sigils, Fibonacci-9, hermeticism, "outer-lands/firmament") are AI-generated or historical
aesthetics. Usable only as evocative internal naming (Venturi, DRUM, collapse-gate read well). The physics
claims in them (EZ-water qubits, 3-6-9 vortex, matter-as-slowed-light, flat-earth firmament) are contested or
pseudoscience — never source, never product copy.

## NET
One real deliverable BUILT (venturi_throat, 5/5, wired, gate 0-broken). The synthesis is a genuinely useful
MAP of the estate that happens to point at real research areas — but the specific paper ids and the novelty
claim need a browser to confirm before we build to or cite them. Held the line: sibling's unverified citations
are leads; the myth imagery is naming flavour; only the tested atom is fact.
