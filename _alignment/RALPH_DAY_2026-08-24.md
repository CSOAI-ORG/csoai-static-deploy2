# RALPH DAY — 2026-08-24 (JEEVES lane, sovos overnight) — [running ledger]
_Per-item DONE / BLOCKED / GATE. Provenance in NIGHT_PIPELINE_LEDGER_2026-08-24.md + RALPH_ROUND_LOG.md. UNMEASURED never 0._

## P1 VERIFY NIGHT PIPELINE
- ✅ Corpus 257 (was 214 at header; grown live). Provenance: `sov_grpo_training_data.json` mtime 24 Aug 03:57, `overnight.log` corpus= lines.
- 🔴 train-v3 produced NO output through the night (ephemeral `/tmp` venv purged → launch died every pass). Now re-launched on persistent venv by concurrent lane (launchctl com.sovos.train) — RUNNING, UNMEASURED until output-v3 + eval_student verdicts.
- 🔴 eval_student verdicts (v3/v4): NONE yet (chained after train).
- 🔴 EAT proof (21:21 + 03:00): dead — sink pod EXITED → crons not firing (UNMEASURED).
- 🔴 corpus-214 sync to pod: FAILING (sink down, rsync code 255 each pass). Mac corpus = 257.
- ✅ Monorepo remote HEAD `e9910602...` on `main`; gateway v2 live on :8878.

## P2 CEO MONDAY OPS
- ✅ `fleet.json` refreshed to live (2 RUNNING $1.61/hr; sink EXITED; 5 volumes; 23 serverless; $5.32/hr; balance $96.33).
- ✅ `~/.ssh/config` `sovos-light-a100` port 23166→**14954** (verified OPSCONNECT_OK).
- 🔴 Spend reconcile: **$5.32/hr (account) vs $1.61/hr (pods)** — unaccounted ~$3.71/hr = warm serverless workers (workersMin=1) + disk.
- GATE sink DOWN → offload/EAT/retrain host. **Critical.**
- GATE mine A100 RUNNING vs `do_not_start`.

## P3 MODEL LOOP
- 🔴/⏳ training v3 in-flight (concurrent lane handled venv fix + launch). Do not duplicate. Next: verify completion + gates (GOVBENCH 0.931 / DEFBENCH refusal 1.000 overblock 0.000 / COMPBENCH 84.5%).

## P4 FREE OWEM ROUTER
- ⏳ `sov_router_fast.py` 23-axis RUNNING on 3090 (`194.26.196.156:23243`); `free_sov_router.json` not yet written. Next: persist + wire `/api/model-router`.

## P5 SECONDARY
- Not yet started this round (oowm-gateway PLAN_100_STEPS + WEBSITE_PENDING_WORK are next-round candidates).

## P6 DAY-END CONTRACT
- OPEN until spine done or every incomplete item names a person/gate. Currently several items name GATE (sink restart, mine decision, serverless redeploy, key rotation).

---

## ROUND 2 (r2, ~04:25 UTC) — verified deltas
_Fresh worker, round 2 of 20. Re-verified against workspace; prior round's findings corrected where reality diverged._

### P1 / P3-D — training PIPELINE IS NOW HEALED (round-1's biggest red item)
- ✅ VERIFIED ROOT CAUSE (round 1): driver used ephemeral `/tmp/sovtrain/bin/python` (purged by macOS tmp-purge) → every launch died `no such file or directory` → no output dir. **FIXED PERMANENTLY by sibling lane**: persistent venv `~/.clawd/.venv-sovtrain/bin/python` (py3.11.15 / torch 2.8.0 / tf 4.57.6, proven) + dedicated launchd job **`com.sovos.train`** (`sovos-train-v3.sh`, RunAtLoad, KeepAlive=false, survives session reaping + tmp purge).
- ✅ **Training RUNNING**: PID **72829** `sov_minimal_train.py --steps 150 --output sov-minimal-output-v3`, alive (1.0% inst / 1:54 CPU accumulated), persistent venv. Log shows `Loaded 332 training examples` + `Starting training for 150 steps...`. **No output dir yet** (saves at end). Did NOT launch a duplicate (rule: never two concurrent Mac trainings). CPU-time accumulation confirms progress.
- 🔴 **HONEST GAP**: launchd `sovos-train-v3.sh` hardcodes `--output sov-minimal-output-v3` and loaded the **332-ex** snapshot at launch (04:09). Corpus is now **481**. So v3 trains on a 332 snapshot, not 481. The driver (auto-version) will launch **v4 on a fresh/full corpus** after v3 completes (it only fires when `pgrep -f sov_minimal_train` is empty). Documented, not a failure — the retrain cadence is naturally sequencing v3→v4.
- ✅ Corpus growth: **308 → 456 → 481** (deepseek-only teacher; openai/groq/together/mistral/perplexity/google/anthropic all 401-stale = rotation GATE).

### P2 — serverless burn ATTRIBUTED (precise vs round-1's qualitative)
- ✅ **6 endpoints keep `workersMin=1`** (warm worker = continuous per-second GPU billing): `sov-fallback-qwen25-3b`(ysz9umv6vu87jm), `sov4-gemma2-9b`(erwy1ybif4vhfj), `sov4-mixtral-8x7b`(8f8g3472aswn1e), `sov4-olmoe-1b-7b`(8w0bbdxw5wc2a2), `sov4-qwen25-7b`(2nvfnpy2jvhtqj), `sov6-kimi-k3-2tb`(c22oxi45hjf9rc). 17 endpoints have no `workersMin` (presumably 0 / scale-to-zero). **This is the unaccounted ~$3.71/hr delta** (account $5.32 vs 2 pods $1.61). ✅ Honest mechanism confirmed: warm serverless, not the pods. Fix (set workersMin=0 / tear cold endpoints) = **GATE (Nick)**.
- 🔄 **CORRECTION (major, supersedes round-1 "sink DOWN")**: sink pod `sz0duht9e5bbov` was **Resumed by user 2026-08-24 03:23:33 UTC** → now RUNNING (verified: `SINK_33982_OK`, hostname `af3455f37658`, up 485d, **loadavg 172 / 156 / 156** on 2 vCPU = still severely overloaded). Its **SSH port CHANGED 25804 → 33982** (runpodctl `ssh_command` authoritative). The overnight driver (`sovos-overnight.sh`) was still syncing to stale **25804** → that (not "pod down") is why rsync kept failing with code 255. **A sibling lane has already patched the driver to port 33982** (verified: all 4 rsync/ssh lines now `-p 33982`). Offload archive intact: `/workspace/offload-dsh/clawd` = **1414 files**; `/workspace/sovos-eat.sh` + `/workspace/eat-logs/` (cron-tick, finish-watcher) present. → Offload sync + EAT proof cycle can resume on the next driver pass (post-fix); verify pass>03:36 in overnight.log. **Remaining risk**: loadavg 172 → keep sink ops minimal (doctrine); port may rotate again on future restart → consider dynamic-port resolution.
- 🔴 **GATE (owner=Nick), still true**: sink is re-attached on a new port but its `lastStatusChange` shows a USER manual resume; the earlier "not enough free vcpu" quota gate was the blocker that has SINCE cleared (resume succeeded). Confirm the sink resume was intentional + re-add the daily EAT/backup wiring if needed.

### P4 — FREE OWEM ROUTER (Block 1, in-flight)
- ⏳ Router deps verified READY on 3090: `/workspace/banks-all/` has **36 `gspc-*.jsonl`**; `/workspace/sovos-repo/benchmark-results/` exists; bench Ollama `:11435` has both **`sov33-unified:latest`** and **`qwen3:4b-8k`**. `free_sov_router.json` **NOT yet written** anywhere.
- ⏳ Re-launched `sov_router_fast.py` (23-axis × 2 models = 46 calls) **detached** on the 3090 (`setsid nohup`, log `/workspace/sov_router_fast.log`, writes `/workspace/sovos-repo/benchmark-results/free_sov_router.json`). SSH to the 3090 timed out during the launch wrapper (**cooldown ≥20min per doctrine**); the process launch is UNCONFIRMED — verify after cooldown, do NOT re-probe until then.
- ✅ P4 wiring groundwork observed: `oowm-gateway/oowm-router.json` already carries a per-axis routing table + **honest routing-gain finding `+0.003`** ("sov33-unified is such a dominant all-rounder that specialist routing adds only +0.003") + provider map. This is the target for merging `free_sov_router.json` into `/api/model-router`.

### P5 — secondary
- 🔵 Delegated read-only research on WEBSITE_PENDING_WORK (logo deploy / Kaggle handle / MCP-registry llms.txt truth / IndexNow / schema.org Dataset JSON-LD) + oowm-gateway PLAN_100_STEPS locally-doable steps to a background subagent. No file writes from the subagent.
