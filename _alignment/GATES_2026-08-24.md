# GATES — 2026-08-24 (JEEVES lane) — FLAG ONLY, owner = Nick
_Never decide these. Each is a measured, honest flag with provenance. UNMEASURED never 0._

## P0 — Runtime/billing (account-affecting)
1. **Mine A100 `l7g747oivyq6ab` RUNNING vs fleet `do_not_start`** — $1.39/hr ≈ $33/day. `lastStatusChange` = "Resumed by user Sat Aug 22 10:45:10 GMT". Keep or stop is an owner decision (this round did NOT change it). Owner=Nick.
2. **Account spend over limit** — `currentSpendPerHr` **$5.32/hr**, `clientBalance` **$96.33**, `spendLimit` **$80** (already past the alert threshold). Runway decision. Owner=Nick.
3. **RunPod serverless warm workers** — **6 endpoints keep `workersMin=1`** (continuous per-second GPU billing): `sov-fallback-qwen25-3b`(ysz9umv6vu87jm), `sov4-gemma2-9b`(erwy1ybif4vhfj), `sov4-mixtral-8x7b`(8f8g3472aswn1e), `sov4-olmoe-1b-7b`(8w0bbdxw5wc2a2), `sov4-qwen25-7b`(2nvfnpy2jvhtqj), `sov6-kimi-k3-2tb`(c22oxi45hjf9rc). This is the **unaccounted ~$3.71/hr delta** (account $5.32 vs 2 pods $1.61). Fix (set workersMin=0 / tear cold endpoints) = console redeploy. Owner=Nick.
4. **API-key rotation** — openai/groq/together/mistral/perplexity/google/anthropic all return **HTTP 401**. Only deepseek contributes to the corpus. Owner=Nick.

## P1 — Infra (estate reachability)
5. **Sink pod `sz0duht9e5bbov` resumed on NEW SSH port 33982** (was 25804) at 2026-08-24 03:23:33; **loadavg 172 / 156 / 156** on 2 vCPU (severely overloaded). Driver patched to 33982 by sibling lane. Confirm resume was intentional + re-add daily EAT/backup wiring; keep sink ops minimal. Owner=Nick (confirm) / lane (keep-minimal).
6. **RunPod serverless redeploy for 500/cold endpoints** — `sov6-*`, `sov4-qwen38-27b`, `sov4-r1-7b` reported 500/cold per 23 Aug. Owner=Nick.

## P4 — measurement decisions
7. **sov-router 23-axis measurement** — `free_sov_router.json` not yet written (measurement launched on 3090; in-flight, verify after SSH cooldown). No gate; status only.
8. **oowm-router WORKHORSE drift** — `oowm_router.py` WORKHORSE=`qwen3:8b` but `model_registry.json` marks qwen3:8b **DROPPED FROM POD** → fallback may point at an offline model; needs reconciliation into `/api/model-router` wiring. Owner = lane (reconcile), flag for review.

## Owner = Nick (human) — summary
- mine A100 keep/stop · serverless workersMin=0/redeploy · balance/spend-limit runway · API-key rotation · confirm sink resume & EAT/backup wiring.
