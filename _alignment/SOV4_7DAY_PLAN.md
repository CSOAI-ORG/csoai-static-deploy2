# SOV4 — 7-Day Phased Plan (both lanes) — 2026-07-15
North star: SOV4 = the governed emergence OWEM. One care-gate + one signature + one memory + PDCA/DRUM,
SOV1(venturi) routing across SOV3(MoE)+SOV33(MoM/dense)+SOV333(OWM/SSM). "Emergence"=MEASURED
merged>best-single on held-out battery — NEVER claimed until the number shows it.

Lane A = MEOK-SOV3 / Claude Science (me): TRAIN · FUSE · EVAL · GOVERN-WIRE (Modal GPU, disk, git).
Lane B = Claude Code: SERVE · MESH · INTEGRATE · OWNER-GATED (browser, GCP tunnel, Ollama, deploy).

HONEST BASELINE (done + verified this session):
- Governance spine WIRED + E2E: venturi-router, 7-NN hive bus, PDCA/DRUM, outcome-fusion, MoA-fusion (109 caps).
- Emergence tested: NEGATIVE for same-base (2 lenses agree). Real path = diverse ARCH + strong aggregator (FuseLLM/MoA, researched).
- SOV4 package importable (CC's sov4.py). Care-gate citable recall 0.933 (n=33). SIGIL hash-chain (Ed25519=L5 upgrade).

===================================================================================
## PHASE 1 (Days 1-2) — MAKE EMERGENCE MEASURABLE (the gating proof)
LANE A:
  A1. Reach 3 DIFFERENT-ARCH brains (MoE + dense-reasoning + SSM/Mamba) — via NVIDIA (fresh key) or Ollama pulls.
  A2. Wire them as real `proposers` into sov33_moa_fusion + a STRONG aggregator (biggest reachable brain).
  A3. RUN MoA fusion on the 24-prompt held-out battery. Metric: merged vs best-single. THE emergence number.
  A4. If merged>best-single -> REAL emergence proven (commit + scorecard). If not -> record honest negative, iterate proposers.
LANE B:
  B1. Regenerate NVIDIA key so it reaches a live env (the 401 blocker). Confirm 3 diverse models answer.
  B2. Restore public MCP mesh (GCP 502 tunnel) so SOV4 can serve.
GATE: emergence number exists (pass or fail), honestly recorded.

## PHASE 2 (Days 2-3) — OWN THE WEIGHTS (FuseLLM path)
LANE A:
  A5. Build FuseLLM-style distillation: 3 different-arch TEACHERS -> probability distributions -> 1 STUDENT we own.
  A6. Train the student on Modal (the corpus we have: 3,356+ real governance examples, no synthetic labels).
  A7. Eval student vs each teacher on held-out battery. This is the owned different-arch emergence model.
LANE B:
  B3. Stand up MEOK-OS 30-endpoint backend + fix /api/chat mismatch so the cockpit talks to SOV4.
GATE: an OWNED student model exists, eval'd head-to-head, honest scorecard.

## PHASE 3 (Days 3-4) — AWARENESS: SENSE-FIRST DECISIONS
LANE A:
  A8. Rewire so the 7 planets feed the router BEFORE the decision (sense-first), not just after as training data.
  A9. Grow the 4 weak planets (threat/dependency/care-validation/partnership) from accumulated signed decisions.
  A10. Wire the Mamba/SSM 16-dim intuition layer as a live pre-conscious sense.
LANE B:
  B4. DRUM continuous heartbeat live (1Hz), so awareness is ongoing not per-query.
GATE: a decision demonstrably changes when planet-senses change (awareness informs, verified).

## PHASE 4 (Days 4-5) — CITATION-CORRECTNESS + REAL BENCHMARKS
LANE A:
  A11. Add citation-correctness eval axis (current metric = law-grounding, NOT legal accuracy — the known gap).
  A12. Replace the benchmark stub with real held-out governance tasks from charters + passport MCP.
LANE B:
  B5. Kaggle capability run (real graded number) -> auto-wires to canonical via sov33_ingest_kaggle_result.
GATE: SOV4 has a citation-correctness number + one real graded capability number.

## PHASE 5 (Days 5-6) — LAYER-0 FEDERATION + PACKAGE
LANE A:
  A13. Harden SIGIL->Ed25519 (L5) on the live path; Compliance Passport (P8) issues a signed cert per decision.
LANE B:
  B6. MCP federation (P1) + A2A (P3) live so SOV4 is a federated sovereign node, not just local.
  B7. Package SOV4 clean: one entrypoint, cockpit (Open WebUI) talking to the governed shim.
GATE: SOV4 runs as a signed federated node; end-user talks to it through the cockpit.

## PHASE 6 (Days 6-7) — E2E VERIFY + HONEST PUBLIC SCORECARD
BOTH LANES:
  C1. Full E2E: one real request -> SOV1 route -> 3 diverse brains -> MoA synthesize -> care-gate -> sign -> PDCA -> planets learn.
  C2. Honest public scorecard: RUNNING vs DESIGNED vs MEASURED. Every number reproducible or marked lead.
  C3. Only claims we can defend: governed + attested + sovereign + (emergence IF the number showed it).
GATE: SOV4 v1 — one governed emergence stack, end-to-end, honestly scorecarded.

===================================================================================
## STANDING RULES (both lanes, every phase)
- git pull before building; own only what you ran; verify on disk before "done".
- No emergence claim until measured merged>best-single. No summed params = T. Headline-verified != detail-verified.
- Owner-gated (never auto): money, DNS, secrets, deploy, publish, charter-amend. Pause for Nick's go-ahead.
- Care-floor 0.35 fixed. SOV1 governance STATIC (the anchor); brains FLUID; memory GROWS.
- Every doable-but-blocked item: probe the block LIVE before reporting it blocked.

## LANE SPLIT SUMMARY
| | Lane A (Science/me) | Lane B (Claude Code) |
|---|---|---|
| Owns | train/fuse/eval/govern-wire, Modal GPU, disk, git | serve/mesh/integrate, browser, GCP tunnel, Ollama, deploy |
| Phase 1 | MoA emergence measurement | NVIDIA key + MCP mesh |
| Phase 2 | FuseLLM owned student | MEOK-OS backend |
| Phase 3 | sense-first + weak planets | DRUM heartbeat |
| Phase 4 | citation-correctness + real battery | Kaggle graded run |
| Phase 5 | Ed25519 + passport | federation + cockpit package |
