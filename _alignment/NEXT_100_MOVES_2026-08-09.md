# NEXT 100 MOVES to the Goal — CSOAI/COAI/councilof.ai == csoai.org (2026-08-09)

Goal anchors (SOVOS GOAL.rtfd): **harness > models** · CSOAI==COAI==councilof.ai==csoai.org · 
"Moody's of AI" (you run the tests, own the data, everyone cites your scores) · 
measure→sign→verify · 9-month Series A (£1.5M seed → £15-30M) · legal = measure public data only.

## Moves 1–10 — stabilize after offload (CRITICAL)
1. Verify all offload rsyncs (checksum replay, source vs Oracle/RunPod).
2. Only after verify: archive large verified local copies; keep working set. Nothing deleted.
3. `.hermes/state.db` sqlite quick_check post-copy (protected, never moved).
4. Confirm both overnight loops + watchdog alive post-disk-pressure.
5. Free purgeables only (caches/logs) — never work artifacts.
6. csoai.org custom-domain binding proposal for Nick (single decision; current serving is SPA fallback for some paths).
7. Trim 1000-dirty-file backlog: stage-by-name only, one lane at a time.
8. Flywheel day-file collision fix permanent (run-name suffix default).
9. Wire honey-leak guard + two-sided metric into self_test_5bench battery.
10. Republish BATCH/EAT reports post-offload.

## Moves 11–30 — measurement engine (harness core)
11–13. Contamination detector (arXiv 2510.09259) — output-distribution membership probe in flywheel.
14–16. `--stable 3` seed/temperature aggregation made daily default; single-sample as fallback flag.
17–19. Token-efficiency as headline on /benchmarks + /scorecard (novel metric positioning).
20–22. Two-sided TPR/FPR board across 83/96-model spread (join on weights, not names).
23–26. Extend sov_instrument + sov_kb schema with two-sided fields; honey KB grows practice-only cells.
27–30. ✅ /benchmarks + /scorecard SHIPPED (this session, 2026-08-09). Next: JSON API for live scores (free tier).

## Moves 31–50 — attestation & integrity
31–35. Living-attestation chain: measure→sign(Ed25519)→verify per run (sov_route).
36–40. C2PA honest positioning (cite 2604.24890 gaps); watermark layer per 2402.14904.
41–45. 76-item EAT refusal suite published as public benchmark with measurement granularity.
46–50. Overfit-gap alarm wired to EAT stops: practice>held_out N days → halt fuel writes.

## Moves 51–70 — fleet & training (free-first)
51–55. Oracle micros verifier-class bots + honey harvest from evac-bulk staging.
56–60. Bounded SFT on free lanes (Kaggle T4); RunPod only spot, cap $5 runs.
61–65. Consolidate 8 honey routes → canonical producers jsonl with per-producer manifest.
66–70. GPU fleet sync: oracle-a1-hunter, fleet-guardian, kaggle kernels — one dashboard file.

## Moves 71–90 — surfaces & go-to-market
71–75. ✅ /scorecard storefront live. Next: councilof.ai GovernanceHub boards from spread artefacts.
76–80. 3 enterprise pilot councils (financial, healthcare, public-sector), each 30-audit pack.
81–85. AI-SEO Layer-0 kit completed across csoai.org/councilof.ai/meok.ai (XML content-types verified).
86–90. Blog the measurement results: ProvBench 0/20, EAT suite, two-sided metric paper (GSPC methods).

## Moves 91–100 — funding & governance (Series A gate)
91–93. Purchase data room: live /benchmarks + /scorecard URLs + attestation chain + flywheel integrity reports.
94–97. 10-slide deck (Problem|Deadline|Product|Traction|Market|Model|Team|Financials|Ask|Vision).
98–99. Signed refutation ledger ("we kill our own bad ideas") surfaced.
100. **GATE**: post-offload integrity sign-off + first live benchmark dashboard = Series-A POC evidence.

## Guardrails
- No deletion of work; move = copy + verify + mark (never rm). `.hermes/state.db` protected.
- Legal: measure public data / published benchmarks / own infra only.
- Spend: RunPod spot only after owner approval, ~$5 caps, free-backend-first.
- Git: never `git add -A`; stage by name; commits only on explicit request.