# 🐉 SOV3 · SOV33 · SOV333 — the three models, one clean integration (2026-07-14)
_Forged from the global intel (1,627 models scanned) + this session's tuned mechanisms. Three models by SCOPE,
each wired to REAL open bases + our measured-best config + full governance. CPU-proven; LLM-scale = staged GPU recipe.
Run: `python3 sov333_trinity.py` → `sov333_trinity_results.json`._

## The three (built + signed, CPU proof each)
| model | scope | real bases | tuned config | governance | runs on |
|---|---|---|---|---|---|
| **SOV3** | small council · one person | **Qwen3-0.6B/1.7B** (Apache) | 4-brain council @ small tier | care-gate + Ed25519 SIGIL | **16GB Mac TODAY** |
| **SOV33** | routed + world model · tools | Qwen3-4B/32B + **DeepSeek-R1** (distil) | route small→large on mirror-divergence + governed world-model plan | care-gated-BFT · mirror-auditor · SIGIL seam | 48GB Mac / cloud |
| **SOV333** | nested + frontier · sovereignty | Qwen3 experts + **DeepSeek-V4 1.6T** (MIT) + **V-JEPA2** | **12×4@nu0.7** pyramid · 4-around-1 nested · reputation-BFT to 5/9 · frontier escalation | care-gated-BFT · OSCAL · BFT 23/33 · SIGIL ledger | server / frontier GPU |

## Every piece is a measured artifact this session
- Topology: `pyramid_best_config` (12×4@0.7 = 0.0311) · `pyramid_4brain` (+48%) · `nest_regions` (regional law)
- Routing: `route()` + `mirror_auditor` (divergence↔error 0.434, escalate-to-frontier)
- Governance: `care-gate` (40/40 red-team) · `governed_robustness` (#1, holds 5/9 tuned) · `venturi_throat` (tamper-caught) · OSCAL
- World model: `sov33_world_model` (dynamics/plan/counterfactual + care-gated transitions) + honest chaos limit + abstain-fix
- Merge: `brain_merge_laws` (route/distill, soup same-base only) → MergeKit at scale
- Local runnable: `sov33_local_sovereign` (SOV3 today) · `sov33_composition_demo` (SOV33 world-model decision)

## The bleeding-edge intel wired in (from the 1,627-model scan)
- Base = Qwen3 (well-chosen, 27M dl) · reasoning distil = DeepSeek-R1 · MoE ref = DeepSeek-V3 (103K★)
- RAG embeddings to adopt = **BAAI/bge-m3** (35M dl) · world model = **facebookresearch/jepa / V-JEPA2**
- Post-train = TRL (SFT→DPO→GRPO) · merge = MergeKit · serve = vLLM/SGLang · SSD-stream = MoE-Infinity

## Honest boundary (the one true limit)
CPU proofs instantiate the **governed topology + signing** per tier (real, small-scale). The **LLM-scale training**
of the three (QLoRA the Qwen3 experts, soup, stack 12×4@0.7, distil DeepSeek-R1, wire V-JEPA2) is the **named GPU
recipe** in `SOV33_GPU_BUILD_SPEC` — staged, one owner GPU run from real. We don't fake-train on CPU; we prove the
laws + governance and stage the scale build. **Three models: defined, integrated, governed, signed — and each has
an exact, real-base recipe to become the LLM-scale version.**
