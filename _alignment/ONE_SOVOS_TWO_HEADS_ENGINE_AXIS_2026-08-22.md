# ONE SOVOS — Two Heads, One Engine-Axis that Measures + Signs Everything (2026-08-22)

Alignment doc (JEEVES lane) — for all agents. Grounded in what's VERIFIED this session.

## The embodiment (your framing, confirmed)
- **CSOAI = the BODY** — certification, attestation, governance, the compliance/legal/certification
  layer (30-framework crosswalk, proof-of-ai/C2PA, BFT council, ISO 42001).
- **MEOK = the PUBLIC AI head** — gaming/consumer AI front-end (AGUI live; "dragon companion" worlds).
- **SOVOS = the single sovereign** — one memory, one signature, one care-gate, one measurement engine.
- Two heads, one SOVOS: the consumers play MEOK; the world trusts CSOAI; SOVOS verifies both.

## The Engine-Axis: one agnostic machine that measures + signs EVERYTHING
Verified this session: the **axis engine** runs live measurements (GSPC axes → honest verdict → signed
record), the **gate** (`dorado_gate`) is the deterministic safety referee, the **flywheel** turns runs
into data → fuel → retrain → re-measure. It is **domain-agnostic** — the axes (governance, safety,
provenance, conformance, openness, continuity + the 15-axis GSPC plan) measure ANY subject:
| Subject (the "what we measure+sign") | Environment (MCP pack) |
|---|---|
| AI model/lab | model arena (meok_arena, verified) |
| **Bond/structured trade** | `finance-mcp-pack` (bond tokenization, DvP settlement) |
| **Insurance claim/underwriting** | `insurance-mcp-pack` (care-membrane scoring) |
| **COBOL batch → settlement** | `cobol-a2a-bridge-mcp` (COPYBOOK → C2PA attestation) |
| **Index/stock market process** | `markets-mcp-pack` (quote integrity, price provenance) |
| **Bank wrapper/agent** | `bank-bridge-mcp` (identity + compliance + attestation) |
Each environment feeds the same axes → verdict → **signature**. One engine, many environments.

## The flywheel is the universal friction-solver (the middle)
Your insight is right: the flywheels literally solve the blockers/friction in the MIDDLE — for every axis:
```
LIVE RUN (any domain) ──> MEASURE (care+gate+compliance on the 15 axes)
   ──> VERDICT+PEER (sigilled/C2PA) ──> DATA (traces, preference pairs, failure incidents)
   ──> FUEL ──> RETRAIN the sovereign ──> RE-MEASURE ──> improve → run again
```
The flywheel doesn't care whether the runner is a bank settlement, an insurance claim, a bond quote,
or a dragon companion — it **measures, attests, and feeds back**. That's the moat: OpenRouter/DTCC/
Bloomberg have no measurement loop; we do (verified running).

## What to BUILD next (the new domain axes) — ordered by the winning math
1. `cobol-a2a-bridge-mcp` (the ATOMIC unit per the bond-dive): COPYBOOK parser → C2PA attestation →
   ISO 42001 probe on the batch job → DID mapping. One bank pilot. (The "wrap, don't replace" = 85%
   of $3T replacement projects failed — wrapping wins.)
2. `finance-mcp-pack` + `markets-mcp-pack`: bond tokenization DvP + quote provenance → fed into the
   15-axis engine (provenance + conformance axes).
3. `insurance-mcp-pack`: underwriting/claims → care-membrane scoring (the ethics axes).
4. The **settlement fee + compliance SaaS + data licensing** revenue streams ride the verified
   gateway/ledger (EUNOMIA `:8877` + commission ledger, built + live).
5. Two heads: keep MEOK (public AI/gaming, AGUI live) generating the participant data; CSOAI certifies.

## Alignment note for the other lanes
- MEOK lane: the games ARE the environment-generator (NPC interactions = micro-benchmarks). Keep them
  on the AGUI/MEOK head.
- CSOAI lane: you're the body — attestation + certification stays the trust layer (don't rename to
  EUNOMIA; "CSOAI" is the brand, "EUNOMIA" is the router protocol).
- Infra lane: warm the RunPod workers (671b + sov4 console redeploys) — the verified gateway/arena are
  ready; warm compute is the only gate.
