# OPENAI APPS IDENTITY VERIFICATION — PREP DOC (NEXT-100, 19 Aug 2026)
**Why now:** OpenAI Apps identity verification has a **30–120 day queue** (canon
TODAY war plan Stage 3: "file EARLY"). Filing now means the identity lands in the
window for the Phase-2 aperture work (MCP Apps reach into ChatGPT — estate map §5).

## What it is
OpenAI's verification of an app/agent identity that can appear inside ChatGPT
(MCP Apps SEP-1865 `ui://` surfaces). Without it, our council-space card view
can't reach ChatGPT users.

## What Nick needs to do (the only human step)
1. Log into the OpenAI platform account (the one tied to the org's apps)
2. Complete the identity verification flow (business entity + domain control)
3. Domain TXT verification for csoai.org (and/or councilof.ai)

## What I've prepared (agent-doable)
- The MCP Apps manifest is live: `csoai-agui-wire/.well-known/mcp.json` (SEP-1865
  `ui://` council-space resource + gspc.axes/verify tools) — the app that will
  carry the verified identity.
- Domain TXT record value: generate once Nick picks the platform account
  (TXT is a DNS change = Nick's registrar access).

## Sequence (do now)
1. [ ] Nick: confirm which OpenAI platform account is the org's
2. [ ] Nick: start identity verification (entity + domain)
3. [ ] Nick: add the TXT record (I supply the value once the flow generates it)
4. [ ] Lane: after verification, wire the council-space app into the MCP Apps flow

## Why it compounds
- 30–120 day queue = filing today puts identity in place by ~Nov-Dec = exactly the
  Phase-2/3 window (Art 50 countdown, x402 meter, summons).
- "File early" is the only way to make the queue work FOR us.

## Firewalls
- The app surface is the fixed-catalog renderer (never free-form HTML)
- Verification = identity proof, never an endorsement/certification claim
- Measurement-not-certification register on every surfaced card

*Prepped by JEEVES (K3), 19 Aug 2026. Nick: start the flow; I handle the wiring.*
