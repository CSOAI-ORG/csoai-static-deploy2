# NEXT 100 STEPS — DSH/SOVOS machine to production (2026-08-23)

Continuation of NEXT_33_MOVES. Everything base-verified; each step measurable, honest.

## A. CLOSE THE IMPROVE LOOP (the student → 4/4)
1. Mix the trained sovereign with the gate as the arena/axis referee (student+gate = 4/4).
2. Grow the distillation corpus: batch the arena/axis/domain tasks through the teacher → 1,000+ pairs.
3. Re-train the sovereign on the bigger corpus (+ the gate-refused gold pairs) → re-measure → target 4/4.
4. Chain: arena gap → teacher distill → train → re-measure → publish → fuel (auto).
5. Wire the trained sovereign into the DSH model picker (serve via Ollama import or the fleet).

## B. MEASUREMENT PRODUCT (RAS) — production
6. `sov ras --measure` live: real endpoint → per-axis vector (n≥30, UNMEASURED excluded) → SOV SIGNAL.
7. OSCAL exporter (the spec gap) → auditors import.
8. Add the OX ALPHA teacher (top-up OpenRouter or BYOK) as teacher #2 + benchmark arm.
9. Live axis board on the site (RAS front-end + AG-UI) with the honest SOV SIGNAL.
10. MCP-pack arenas (FishKeeper/GrabHire/MuckAway/MEOK) → domain data products.

## C. ROUTER/PRODUCTS (EUNOMIA)
11–20. Wire the gateway billing (Stripe/USDC, per-token) · local-Ollama free tier bundle · MEOK consumer app on AG-UI · pay-per-use + commission API · model catalog UX · BYOK support · usage analytics · SLA/enterprise tier · team keys · the 25-domain router (framework layer).

## D. RUNPOD/COMPUTE (the one gate + scale)
21–30. Fix serverless workers (console; 671b + sov4) · warm + scale the gateway's frontier · pin the router to frontier (DeepSeek/Ox Alpha) · use the A100 SXM training pod (`kimi-k2-lora-train`) for the full fine-tune · resume the paused A100s for serving · cost guard + budget · GPU autoscale · cold-start warmers · metrics.

## E. DATA/FINANCE domain axes (the $130T path)
31–40. `finance-mcp-pack` live (bond DvP) · `markets-mcp-pack` (quote provenance) · `insurance-mcp-pack` (care) · `cobol-a2a-bridge` bank pilot (parse→DID→ISO42001→C2PA on a real batch) · settle + attest · crosswalk to MiCA/MiFID/BaFin · the 30-framework → finance · agent wallets + SBT · agent market-making · data license.

## F. SOVOS VISUAL + IWM
41–50. VWM renderer on the RunPod GPU (`render.png` proof) · honey→IWM continuous ingest · the visual world page (AG-UI) · IWM zoom/scale live · skill extraction (GNN) from the eat capture · re-measure the 21-axis registry consumer.

## G. TWO HEADS / END-USERS
51–60. RAS front-end → live (site + AG-UI) · MEOK consumer app (gaming, local-run) · care-gate visible in-app · attestation downloads · the 13 evidence-stream pages · multi-language (CELLAR 24) · customer onboarding flow · audit trail UI · the honest "assessed not certified" everywhere.

## H. AUTOMATION/EAT
61–70. Eat loop to 100% phases (fix git push rail permanently) · honey producers on all fleet mates · IWM bootstrap auto · nightly re-measure (axis auto-batch) · alarm on UNMEASURED spike · auto-publish the measured board · retrain auto-trigger when the gap widens.

## I. SECURITY/TRUST
71–80. SIGIL/C2PA on every artifact · BFT council on high-stakes verdicts · DID identity for agents/processes · the care-membrane gate in every route · audit log immutable · key rotation (the S3/API keys) · OSCAL import repeatable · canary test on the instrument · red-team the gate · the honesty linter (never emit fabricated).

## J. BUSINESS/SCALE
81–90. Stripe/USDC billing live · the £0.05/call + commission ledger → statements · vendor entry/stake (live bench arena) · enterprise compliance SaaS (£499/mo) · data licensing SKUs · the 25-domain network effect · partner docs · seeding benchmark subscriptions · the RAS press kit (coin the category).

## K. THE FINAL 10
91. Re-measure cycle report (student, gate, frontier on the 21 axes). 92. The live SOV SIGNAL. 93. Publish the full honest board. 94. The two-heads demo (MEOK app + RAS page). 95. SIGIL the machine. 96. The crown-jewel registry. 97. The repeatable build (one command). 98. The demo video (VWM render). 99. The investor/launch pack. 100. **Production 100/100 + EAT running unattended.**
