# CHARTER — OWEM Four-Scope Semantic Model (what "4 around 1" actually IS)
_2026-07-12. Locks the definition so it never drifts back into "4 different model sizes" or "4× capability".
The 4 nodes are FOUR SCOPES OF ONE GOVERNED SUBSTRATE, connected by SIGIL attestation. NOT 4 models you sum._

## THE CATEGORY CORRECTION (why this charter exists)
The sketch "2 small + 1 med + 1 large around a centre" invites a WRONG reading: that the nodes are model SIZES
and that stacking them multiplies capability or adds tokens. That is the retracted 33T/additive-params error.
CORRECT reading: the 4 nodes are four SCOPES of the same sovereign substrate. Intelligence comes from the base
model(s); composing scopes adds GOVERNANCE + MEMORY + IDENTITY reach, NOT raw capability. This is honest and real.

## THE FOUR SCOPES (semantic definition — each maps to RUNNING code)
| Node | Scope (what it owns) | Real substrate | Status |
|---|---|---|---|
| **Small — AI character** | ONE PERSON: their memory, personality, care-profile; grows with them (accretion) | `capability_owem_world` + `sov33_memory_bridge` (SIGIL-signed, namespaced to one Hatch fingerprint) | RUNNING |
| **Medium — Tools** | WHAT THE CHARACTER CAN DO: the capability/MCP surface (its hands, not a brain) | 76-capability registry + `:3101` MCP cards | RUNNING |
| **Large — SOV33** | THE RULES ALL CHARACTERS OBEY: care-floor, SIGIL, BFT, passport tiers | gates + `sov33_identity` + federation core | RUNNING |
| **Centre — the 1** | THE SHARED SOVEREIGN ROOT all three attest to (makes them ONE system) | the SIGIL chain + charter | RUNNING |

## WHY THIS IS REAL, NOT A DIAGRAM
- **The model is a replaceable organ; the four-scope substrate persists.** Proven structurally: swap the model
  (Qwen→Llama→DeepSeek), the sovereign memory + invariants are untouched (memory lives in the substrate, not the
  model). THAT is why "grows into uniquely yours" works — the growth is in the scopes, not the swappable weights.
- **The character (personal) acts through tools (capability), bounded by SOV33 (governance), rooted in one signed
  chain (identity).** Remove any scope and it breaks: no personal scope = generic bot; no tools = can't act; no
  governance = unsafe; no root = three disconnected programs. The four-scope composition IS the product.

## MANY SMALL OWEMs = THE UNIVERSE (federation, not multiplication)
- Each PERSON'S character is its own small OWEM (own personal scope, own growth). They are NOT copies.
- Two characters meeting = the FEDERATION layer (BFT + reputation + collusion-resistance, sibling-built + tested:
  holds to the classic BFT bound; reputation converts a permanent takeover into a one-shot; upstream lineage
  diversity is the real defence). Many small OWEMs federating under one governance root = MEOK Universe.
- "It grows / new ones emerge / never the same" = each personal scope diverges (proven: 0.78 usage-divergence,
  stable plateau, never converges). Divergence is REAL and measured; it is not "infinite growth" or a mind waking.

## THE HARD LINES (bind this definition)
1. NEVER claim the 4 nodes give 4× capability or additive tokens. They give 4 SCOPES of governance+memory+identity.
2. NEVER claim consciousness/AGI. The composition is a governed substrate; intelligence is the base model's.
3. Scopes are SIZED BY REACH, not by parameters: small=one person, medium=toolset, large=all-characters' rules.
4. The centre "1" is IDENTITY (the signed root), not a bigger brain.

## THE ONE-LINE DEFINITION (canonical)
An OWEM is ONE governed sovereign substrate viewed at four scopes — a person's character (small), its tools
(medium), the shared governance (large), rooted in one attested identity (centre) — where the model is a
replaceable organ and the substrate is what persists, grows, and federates. Real, honest, buildable; not a new
foundation model and not AGI.
