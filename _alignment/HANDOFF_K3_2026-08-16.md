# HANDOFF TO K3 — FULL ESTATE RUNDOWN (2026-08-16, JEEVES lane)

> Read this top-to-bottom before touching anything. This is the canonical state
> so K3 can execute aligned with JEEVES, using all RunPod + Oracle + free-GPU
> lanes, in EAT mode. Brand: **Council of AI** (CSOAI Ltd UK 16939677).
> Doctrine: **measurement, not certification. UNMEASURED is reported, never hidden.**

---

## 1. WHO YOU ARE IN THIS LANE
- **Engine codenames are internal ONLY:** SOVOS, SOV-*, sov6, sov33, OWEM/OOWM,
  JEEVES, Hermes, Kimi. Public surfaces say **Council of AI / GSPC / DEFONEOS** only.
  Leaked codename = brand-gate failure (`councilof-ai/scripts/brand-gate.mjs` bans them).
- You issue **measurement credentials, never certifications.** No "compliance",
  "accreditation", "board-grade" claims. Missing cells = `UNMEASURED`, never zero.
- Multi-lane git: **stage by name, never `git add -A`** (sweeps sibling lanes).
  Verify every commit with `git show --stat`.

## 2. THE BRAND (single truth — `_alignment/BRAND_MASTER_2026-08-15.md`)
> **Council of AI** — we measure AI systems against the rules that govern them,
> sign the result (Ed25519), and publish what we cannot yet measure.
> **Measurement, not certification, and let the record show it.**

Public surfaces must render this verbatim: apex `csoai.org` = measurement home
(title "Council of AI — the measurement body for AI compliance" — VERIFIED 200),
`councilof.ai` = marketing. Go-Machine repo: csoai-static-deploy2.

## 3. LIVE INFRA (probed this session — trust nothing else)
| Asset | Status | Access |
|---|---|---|
| **RunPod 3090** `sov-repull` (`fpowppss5ngtkw`) | ✅ RUNNING + WORKING | `ssh -i ~/.runpod/ssh/runpodctl-ssh-key -p 12853 root@194.26.196.156` |
| **RunPod A100** `sov-brain-a100-fresh2` (`1dldzposn7ssuu`) | ⚠️ RUNNING but SSH banner-timeout — needs **console reboot** | port 11703 dead |
| RunPod `overnight-bench-a100-v2` + `sovos-light-master-mine-20260816` | RUNNING (sibling lanes) | via runpodctl |
| **Oracle 2× E2.micro** `sov33-owem-micro` + `micro2` | ✅ RUNNING, mining (e2e_groq_auto, sigil-emitted) | `ssh -i ~/.ssh/id_ed25519 ubuntu@145.241.232.16`, workspace `/home/ubuntu/sov33_shared` |
| **Google rclone `gdrive`** | 🔴 **TOKEN EXPIRED** — `rclone config reconnect gdrive:` (owner click) | blocks ALL Google backups |
| rclone `sovos` (MinIO localhost:9000) | dead until A100 returns | — |
| Cloudflare Pages `csoai-site` (prod) | ✅ apex csoa.org 200, brand-first | `npx wrangler pages deploy _site --project-name csoai-site --branch main --commit-dirty=true` |
| GSPC MCP | ✅ live `https://csoai-gspc-mcp.nicholastempleman.workers.dev/mcp` (measure+verify) | — |
| PyPI | ✅ 590+ packages live | — |
| HF org `/csoai` | live (29 datasets, 14 Spaces) but display name = old "Safety of AI" — rename gated on `hf auth login --force` | — |
| Kaggle `nicktempleman` | live (33 datasets) but bio "No bio yet" — gated | — |

**RunPod API key** is valid for `runpodctl` (NOT the GraphQL API — Cloudflare 1010 bot-block on raw API from datacenter IP; use runpodctl).

## 4. THE MASTER HARNESS (the mind)
`python3 -m sovos_engine` in `SOVOS/packages/sovos-engine` (run with `PYTHONPATH=packages/sovos-engine/src`):
- **15 engines: 14 GSPC axes MEASURED** (gov 237 items, care 200, all gate=MEASURED, 19-model fleet) + **`owm`** (mined OOWM/OWEM knowledge).
- Commands: `status` / `status <axis>` / `diagnose` / `diagnose <axis>` / `fix --delta`.
- The OWM register (`OWM_TESTED_STACKS`): **6 stacks, 1 real win (JEPA world-model), merges TIE/under base** — honest, don't re-claim otherwise.

## 5. THE MODEL ESTATE (registered, measurable)
Fleet on 3090: `mistral:7b` (BEST classifn gov 0.536 macroF1), `qwen2.5:7b`, `qwen2.5:1.5b`, `qwen2.5:0.5b`, `qwen3:4b`, `council-safe` (CORRECTED: below base), `council-oowm` (0/24 — not certified).
- Full 24-item bench: `_alignment/BENCH_GOV_7MODELS_2026-08-16.json`.
- **Scaling path = base + bank retrieval, NOT 1.5B merges.** Mistral 7B is the production measurement lead.
- 3KB card minted: `_ip/council_GOV.3kb` (3072B) + JSON.

## 6. EXECUTION HARNESS (the batch runner — EAT)
On any pod with the clone: `bash /workspace.master_batch.sh [--bench|--harvest|--cards|--owm|--all]`.
Canonical copy: `_tools/master_batch.sh` in csoai-static-deploy2. It cycles:
bench (7-model gov) → harvest (honey count) → 3KB cards (sov_3kb_converter) → owm status.

## 7. THE 33-MOVE MASTER (current state)
Canonical: `_alignment/MASTER_33_MOVES_2026-08-16.md`. T1-T6 indexed. Done this session:
- ✅ Fleet bench honest (mistral best), council-safe corrected
- ✅ 2 real models registered (council-safe, council-oowm), 3KB card minted
- ✅ owm engine absorbed into the harness (15)
- ✅ Apex brand-first (26 pages, 0 bare-CSOAI leads), GSPC MCP stable
- ✅ Flywheel selftest 19/19 (anti-Goodhart intact), board gov 5214 DONE
- ✅ pandas A1-hunter still hunting

**Remaining owner-gates (the only things that need human clicks):**
1. `rclone config reconnect gdrive:` — Google backup unblock
2. A100 console reboot — restores board stream + MinIO/S3
3. `hf auth login --force` → rename org to "Council of AI"
4. Kaggle bio → Council one-liner
5. councilof.ai `feat/council-rename` merge (sibling-owned worktree — DON'T bulldozer)

## 8. ALIGNMENT RULES (K3 binds)
- Claim a lane in `CLAIM BOARD` — single active agent at a time in clawd tree.
- Never run `git add -A`; never commit onto `clean-main` (sibling POC).
- If you touch a public surface: title brand-first, register truth-tags (REAL/DEMO/THEORY/GATED).
- Never edit another lane's worktree (Mark `/private/tmp/coai-rename` OFF-LIMITS).
- Free-GPU lanes assigned per week: Kaggle T4 (30h/wk) / Colab T4 / HF Spaces / Lightning 22h/mo.
- Oracle micros: never kill the daemon; they selfheal (fleet-guardian every 15 min).

## 9. FIRST 5 MOVES FOR YOU TODAY (EAT mode)
1. `runpodctl get pod` → verify 4 live pods; if A100 still banner-dead, log the reboot action for Nick.
2. `python3 -m sovos_engine status` (with PYTHONPATH) → confirm 15 engines, 0 regressions.
3. Run `bash master_batch.sh --all` on the 3090 → full cycle, capture outputs.
4. Mine free GPU lanes: push one kernel to Kaggle (sov33 training loop exists), verify T4 run.
5. If gdrive reconnected by Nick: `rclone copy ~/clawd gdrive:csoai-backup --max-deep-up 3` (first pass, live-tail).

**Report style: `✅ TASK → 📊 METRICS → ⏭️ NEXT` — handle gate as `🚫 BLOCKED → ❓ NEED → 💡 SUGGEST`.** Honor "measurementnot-certification" everywhere; never print a fabricated number; every claim traceable to rows or a live prob.