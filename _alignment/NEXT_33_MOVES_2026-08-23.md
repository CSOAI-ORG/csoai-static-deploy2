# NEXT 33 MOVES — CSOAI EAT loop compounding + estate readiness (2026-08-23)

Grounding: engine is autonomous + producing (measure→sign→chain→board→mirror), trust chain clean
(315 valid / 0 invalid), board-public live (/api/gspc + /api/axis-register), 17 durable procs.
The gaps: (1) improve-loop genome growth is compute-bound + jail refuses to generate probes;
(2) Zenodo publish blocked by API quirk; (3) owner-gated items pending; (4) node_modules prune
prevents clean rebuilds.

## BLOCK 1 — IMPROVE-LOOP GENOME COMPOUNDING (moves 1-10)
1. Verify genetic_run9 grows genome >177 (rotation fix: jail last). [DONE if grows]
2. If growth flat: increase cycles (5→8) + give the run a longer wall-clock (600s) so more axes complete.
3. Probe the fitness threshold: log the distribution of kcandidate probe fitness values to calibrate the 0.10 cut
   (so discriminative probes are kept, noise killed).
4. Add a non-hanging mutation model for jail/refusal axes (e.g. a bigger instruct model if available,
   else skip jail and document it as an axis that needs an authored bank, not genetic mutation).
5. Seed richer banks for governance/safety/care/accountability (these generate reliably + discriminate).
6. Feed the growing genome back into measure_chain: enqueue jobs from probe_genome so the living bank
   becomes measured (the moat).
7. Hook measure of the genome probes into the EAT chain (sign genome + newly-measured probes).
8. Re-measure the board after genome growth: refresh /api/gspc with the new axis coverage.
9. Stress-test the idempotent singleton: confirm no duplicate genetic process under sustained load.
10. Re-verify the improve loop compounds over 3 consecutive runs (genome strictly grows each run).

## BLOCK 2 — HARDEN THE AUTONOMOUS LOOP (moves 11-18)
11. Restore node_modules (npm ci) so a clean `npm run build:client` can regenerate dist/client (fixes the
    recurring axis-register 404 from stale dist). Requires lockfile-only install.
12. Add axis-register + other /api/* functions to a GHA/deploy guard so concurrent rebuilds can't drop them.
13. Wire the supervisor batch to also run a "board refresh" step (re-sign updated board_living.json).
14. Verify the EAT chain's anchor (ops_daemons RFC3161/OTS) produces a fresh anchor receipt now that boards
    are stable at 315-valid.
15. Confirm board-public is stable across 5 consecutive polls (no 404 recurrence).
16. Mirror the live board/catalog to a 2nd host (Oracle micro) for the independent-mirror box.
17. Add a heartbeat/alerts file the Mac-side dashboard reads (so the loop is observable).
18. Snapshot boot-state: capture the exact env + OLLAMA config that made the engine healthy, so a pod
    restart can reproduce it.

## BLOCK 3 — PRODUCT/MCP/PUBLISH SURFACES (moves 19-26)
19. Publish the signed jail board to Zenodo via the InvenioRDM /api/records publish path (workaround the
    deposit resource_type quirk) -> record a DOI.
20. Register the Zenodo DOI + commit to community "Council of AI (CSOAI)".
21. Re-verify claimguard + council-ledger on PyPI still 0.1.0 + push any new version to reflect board growth.
22. Add axis-register + gspc to the MCP registry catalog (already in mcpRegistry.json; ensure public).
23. Mirror the two product landing pages (claimguard/council-ledger) to the Oracle site for redundancy.
24. Wire /api/axis-register into the AG-UI catalog.html (live production endpoint, already done — re-verify).
25. Publish a signed "board v2" attestation card (the 315-board trust chain head) to councilof.ai/verify.
26. Refresh /catalog.json with the current measured board counts (n per axis).

## BLOCK 4 — OWNER-GATED / ESTATE (moves 27-33) [needs human]
27. GitHub CSOAI-ORG appeal (restricted/404) — require owner action.
28. Rotate the GitHub PAT (P0 security) — owner.
29. arXiv S7VDXA endorsement + Moon submission (7-day clock) — owner.
30. Register cibola.dev / getcibola.com + UKIPO (~£170) — owner.
31. Kaggle phone verification — owner.
32. C2PA disclosures + DIF form — owner.
33. Final: run the EAT 7-box end-to-end audit (measure→CI→sign→chain→anchor→board→mirror), publish a
    signed scorecard to councilof.ai, and confirm 100/100 A++++ gates green.

## Verification doctrine (each move)
- Probe every claim (no assumption). Sign + chain each new board. Honest negatives published.
- All work stays on the pod (RAG volume); Mac stays a thin terminal.
- The EAT loop must remain autonomous (no SSH-dependency, no human) for moves 1-26.

---
## ALIGNMENT CHECK-IN (2026-08-23, JEEVES keep-warm lane)
Cross-checked this plan against live state. Status of the executor-achievable items:
- Move 1 (genome >177): BLOCKED root cause found = model contention from a genetic
  process pileup (5 duplicate gspc_genetic procs) hammering one qwen3:4b, making
  ask() time out (HTTP 000) -> mutate_gap returns 0 probes -> flat genome.
  FIXED: keeper genetic singleton made precise (^python3 gspc_genetic.py) + pileup
  killed; model now responsive (6.5s). One clean genetic run in progress.
- Move 3 (fitness threshold): genetic now runs clean (1 proc) so it can calibrate.
- Move 11 (node_modules): PRESENT, rebuild works.
- Move 12 (/api/* guard): NOT DONE — deploy war keeps dropping /api/cards (404).
  Needs a GHA/deploy guard so functions aren't dropped by concurrent rebuilds.
- Move 15 (board-public stable): /api/gspc 200 (stable) but /api/cards 404 (dropped
  by war). Partially done.
- Move 24 (/api/axis-register): no function exists yet; needs creation + deploy.
- Moves 1-10 improve-loop: unblocked by the singleton fix; genome should compound now.

---
## ALIGNMENT CHECK-IN #2 (2026-08-23, JEEVES keep-warm lane)
Moves 12 + 24 DONE + verified live (deploy 29ccaf9f):
- Move 24: created /api/axis-register (csoai.gspc-axis-register/0.1, 13 axes, "13 of 14" framing).
  Verified live 200.
- Move 12: added Functions bundle guard to deploy.yml (pre-deploy: fails if any required
  /api/*.ts missing from dist/client/functions/api/; post-deploy: functions-guard.mjs asserts
  live /api/gspc, /api/cards, /api/axis-register, /api/mcp, /api/tools all 200). Guard runs GREEN.
- Improve-loop compounding unblocked: root cause was a genetic process pileup (5 dupes) contending
  one qwen3:4b -> ask() timeout -> mutate 0 -> flat genome. Keeper singleton made precise
  (^python3 gspc_genetic.py); model now responsive (6.5s); 1 clean genetic run in progress.
