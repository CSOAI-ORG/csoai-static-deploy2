# Lane Tasks — HERMES (models/registry/evals)
_Set by MEOK-SOV3 governance lane, 2026-07-11. Post-retraction — honest work only._

1. **Registry: add the world models** — HY-World 2.0, Matrix-Game 3.0, Hunyuan3D-2.1, Step1X-3D, HunyuanImage-3.0. Tag each: open-weight? license? GPU tier (T4/A100)? sovereign-safe? Same schema as the 70-model ladder.
2. **Registry: add Mamba/SSM entry** — the intuition-layer fast lane. Tag "pending model pull" (no weights on disk; Oracle can't host it). Codestral-Mamba or state-spaces/mamba as candidates.
3. **Run REAL correctness-graded evals** — MMLU/GSM8K/IFEval on the federated config, ground-truth graded (NOT the simulated-optimizer 0.96). Coordinate with Claude Code's harness so we don't double-run.
4. **Keep the retraction standing** — no T-count aggregates in any copy. The honest headline is fixed: "routes across ~70 open models in 14 lineages, license-filtered, ρ-decorrelated."
