# NEXT 33 MOVES — SOVOS stack → 100% aligned e2e (2026-08-22)

Built on everything verified this session. Honest: each move is measurable; nothing invented.

## PHASE 1 — SOVOS Light Visual Model (OOWM/VWM) → 100%
1. ✅ VWM core: `sovd` emits scene.json (10 nodes, epoch, routed→DeepSeek V4-Pro) — DONE.
2. ✅ VWM renderer build (sov-render, `SOV_FALLBACK=1` software adapter) — running now.
3. Verify `render.png` = the visual proof (nodes + mesh + IWM zoom).
4. Wire `sov-honey` ingestion → IWM (frozen honey → fractal addresses).
5. Publish VWM scene + render to the site (AGUI/visual page).
6. Align `sov-vision`/`sov-qwen-vision` (OWEM) as the perception arm of the VWM.
7. Phase B: real wgpu GPU render on a RunPod pod (per HANDOFF_RUNPOD.md).
8. Phase C: EAT loop (sovereign 3 eat → honey → IWM → retrain → re-measure).

## PHASE 2 — BIG models (the "why small?" fix)
9. Fix `sov6-deepseek-r1-671b` serverless worker (500 — RunPod console redeploy).
10. Verify 671b answers (OpenAI-compat) — the true "large core".
11. Enable `sov6-qwen3-235b` + `sov6-kimi-k3-2tb` endpoints (big models).
12. Point the DSH/sov-core router: general → frontier serverless; governance → sovereign+gate+RAG.
13. Benchmark the big models (MMLU/GOVBENCH) → honest top-tier numbers on YOUR fleet.
14. Wire the axis engine to a big-model arm (measured scores).
15. Manage credits: serverless cold-start budget (balance $116.39).

## PHASE 3 — AGUI + website E2E (100%)
16. ✅ AGUI UI (`:8785`/`:8790` "Council Space") verified 200 — DONE.
17. Confirm AGUI routes/pages all 200 (audit the AGUI surfaces).
18. Wire AGUI → sim data plane (`:4190`) + gateway (`:4191`) e2e.
19. Audit all site pages (sitemap URLs) HTTP 200 on CF Pages deploy.
20. Fix the apex 308 (canonical = CF Pages `csoai-site`; probe surfaces).
21. Publish the VWM visual page + axis scorecards to the site.
22. SIGIL-sign published artifacts (attestation chain).
23. Lighthouse/WCAG pass on the AGUI + site pages.

## PHASE 4 — Axis/measurement loop (auto-batch, honest)
24. ✅ Engine loaded + scheduled (30 min), honest gates verified — DONE.
25. Stage a COMPLIANT arm (instruct) + sovereign control → measured axis scores.
26. Audit the UNMEASURED finding: OWEM is instruction-weak → format-tune it (fuel).
27. Auto-batch: run→audit→improve→run again (the loop continues).
28. 16-axis engine (`axis_engine_16.py`, systemd) on a pod — full axis coverage.
29. Flywheel: axis results → fuel → retrain the sovereign → re-measure (EAT).

## PHASE 5 — Offload/cleanup completion (Mac → RunPod/Oracle)
30. ✅ deploy2 + sim-world-data on the 2.3PB volume — DONE (Mac 9.7G free).
31. Offload `~/.claude` (7.4G worktrees) + coai-dashboard → volume; verify + free local.
32. rclone RunPod-S3 rail (keys stored; endpoint/region verified) for checksummed offloads.

## PHASE 6 — Production 100/100
33. Publish the sovereign OS visual + measurement cards (csoai.org/AGUI), signed, with the honest
   benchmark board (GOVBENCH 0.931 · refusal 1.0 · COMPBENCH 84.5% · MMLU frontier 78.5% vs OWEM 41.5%).
