# AXIS-BOOTSTRAP-EAT — gspc honest register (JEEVES lane, 2026-08-24)

## What is REAL today (stranger-verified from outside, live on csoai-gspc.pages.dev)
- Signed 3KB cards verify without us — stranger-executed content_id MATCH + Ed25519 PASS.
- Public signed board: `12 measured of 13 · jail UNTESTED (earned, never assumed)` — API + frontend gated.
- Machine door without asking: MCP measure/verify/jail-probe/enter-arena + SDK/CLI/widget drop-ins.
- did:web resolves for strangers: `did:web:csoai-gspc.pages.dev#gspc` (Ed25519VerificationKey2020), /.well-known/did.json + agent-card.json.
- Verified-dated regulation deadline API + corrections register + register rollup (/api/registers).
- Per-axis signed card: /api/measure-axis (gov: 0.8, n=30, Wilson 95% [0.627,0.905]).
- SOV Signal index with signed methodology (index-about-index, IY.5).

## Waves landed (commits)
03950a84 JB-D1 pin did:web key · b9736a1d all signers pinned · 481b9605 registers/receipts/agent-card + did.json fix · a09a6409 sign-replay fix + SDK + methodology · bac4e902 jail gate + measure-axis.

## Owner-gated (NOT doable by me — named flip row)
- SOVOS_CHAIN KV binding attach (namespace b4eb1252766040d68bf6b10e6470ab57) → chained:false→true. Cloudflare dashboard action; `wrangler pages` has no KV-binding subcommand.
- P0-1 estate DID (did:web:csoai.org) merge on csoai.org domain.
- Time-anchor (OTS), transparency log, IM.5 gold-set gate, first externally-verified receipt (EXE 009).
- Economy layers (premiums moved / fields adopted / credentials held) — need a first paying/accepting party.

## Boundaries honoured
R8 regulators-free-forever (no 402) · R1/R3 scores-never-sold · ClaimGuard gates public rows · measurement≠certification · '12 measured of 13' (moves only when a signed card lands) · jail UNTESTED · agents = model+harness · never claim traction without a signed stranger-checkable row (JL.5).
