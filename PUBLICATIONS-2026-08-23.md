# PUBLICATIONS — launch post + findings (2026-08-23)

## LAUNCH POST (the distribution hook)
> **"We measured the frontier — and you can verify every result without trusting us."**
>
> Every lab publishes self-reported, unsigned scores. We just measured four frontier base models + our
> own sovereign models on 16 governance axes, on a clean sequential protocol — and **every result is
> Ed25519-signed on a signing node (the key never leaves), verifiable in-browser by anyone.**
>
> **The findings:** ① Retrieved knowledge beats trained knowledge by **+34–38 points** (confound-free).
> ② At the same size, **base models beat our own fine-tunes** — weight-merging weak specialists *dilutes*
> the base (the flaw is merge-not-train, not the model). ③ The whole result is **one signature anyone
> can check** — at `csoai-verify.pages.dev/verify`.
>
> We are the independent measurement body. **Measurement, not certification.** Buyer-side only; never the
> scored. Verify the evidence → `/verify` · Book an evidence pack → `/book`.

## FINDING-1 (the RAG lever — citable)
Retrieved knowledge >> trained knowledge. Clean sequential EAT measured RAG-lift of **+34–38 pts** across
four base models — retrieval compounds, weight-merge dilutes.

## FINDING-2 (the merge-not-train proof — citable)
At identical size, base `qwen2.5:0.5b` (32.6) > `sov33-v7` (19.8) > `sov33-evolved` (11.4). Weight-merging
weak specialists over the base degrades general governance capability. **The estate's own documented
finding, now measured + signed.**

## CHANNELS
- X/LinkedIn: the hook + the `/verify` link.
- HN (technical): the spec (`RECEIPT-SPEC-0.1`) + the verifiable-eval tooling.
- r/MachineLearning + r/artificial: the two findings.
- arXiv/DOI (methodology): clean-sequential + refusal-tolerant measurement.
- The Y-axis essay: "why a regulated instrument can't move human→agent without first climbing verification."

## STATUS (was the "take-our-place" move)
Grounded in live signed artifacts. The spec (`RECEIPT-SPEC-0.1.md`) is the standards-ownership asset;
this launch post is the distribution hook. Both signed.
