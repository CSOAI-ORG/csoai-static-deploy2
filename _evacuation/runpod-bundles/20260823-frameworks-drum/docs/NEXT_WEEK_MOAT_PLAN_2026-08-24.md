# NEXT-WEEK MOAT & GROWTH PLAN (2026-08-24 → 08-30)

> Strategic synthesis from (a) the estate's actual assets, (b) current external landscape.
> The moat is NOT the index or the gauge alone — it's **measured + signed + contamination-aware
> evaluation as the anti-Goodhart compliance asset**. The EU AI Act GPAI obligations are LIVE
> (enforcement started 2 Aug 2026) and benchmark "gaming" is a documented, hot problem — exactly
> the gap a measured layer fills. Doctrine: measurement not certification; [BUILT]/[BET]/[GATE].

## The one-line strategy

**Convert the estate's internal measured gauge into the external, signed, contamination-aware
evaluation evidence pack that EU AI Act GPAI compliance + the anti-benchmark-gaming movement
both demand.** We don't beat LM Arena at being a leaderboard; we own the *verifiable* measured
data regulators and buyers will need.

## What we already own (BUILT — the moat is real, just internal)

- **Live measured gauge** (`sim-world-data/board-live.json`): 30,252 live records, 97 signed
  cards, axes (accountability, affect, …) with model-level mean scores.
- **Measured arena Elo** (`arena-elo.json`): Bradley-Terry (K=32) on our own GSPC pillars, not
  market Elo — 12 head-to-head, ranking sov33-unified 1077, qwen3:8b 1057.
- **Reference index** (the drum, 662 items) + **contamination register** (36 benchmarks,
  12 designed-resistant) + **jailbreak/fleet instrument** (`jail-full`).
- **Bridges** (bond router, insurance engine, data DAO, agent economy) + 22-axis measured scope.

## The clever moves still needed (ranked)

### 1. FUSE the four into ONE public "measured compliance" surface (highest value, [in-lane])
The drum (reference) + board-live (gauge) + arena-elo (arena) + contamination register
(anti-Goodhart) are separate. Fuse into a single public surface that shows: *this model measured
this way, on these pillars, with this contamination status, signed.* That's the product.

### 2. Position for EU AI Act Q4 (Sept-Oct 2026) — [GATE #dsh for signing; [in-lane] for the pack]
GPAI obligations are live since 2 Aug 2026 (transparency, copyright, training-data summary) and
more activate in Q4. The measured evidence pack (signed evaluation, contamination-aware) is the
natural **compliance evidence artifact**. Build the pack + the EU AI Act evidence mapping.

### 3. Finish the TRUST FLIP (the 90/10 router) — [in-lane], blocked only by a quiet GPU
The conformal router is `trusted:false`. A quiet GPU (the pod's 3090 is contended by the sibling
axis engine) unlocks token-entropy coverage → `trusted:true` → the router routes autonomously.
This is the "measure→route→learn" loop fully closing.

### 4. IP: the contamination-aware measured-gauge method — [GATE] Nick + OIN scope check
The anti-Goodhart measured-gauge + conformal-trusted-router method is defensible IP. RUN THE OIN
SCOPE CHECK (it is NOT Linux-kernel-adjacent — measurement, so the OIN grant-back does not apply;
the IP stays ours). Register/fold into the IP posture (already asserted UK/EU database right).

### 5. Connections (grow reach, get warm intros)
- **BSI ART/1 seat** (pending, £0) — the UK mirror of ISO/IEC SC 42; feeds EU AI Act harmonised
  standards. The measured-gauge methodology is the technical contribution = warm intros.
- **OpenSSF / LF x402 / OWASP AI-MCP / AI Verify** — board-membership applications (see
  BOARD_MEMBERSHIP_PLAN). Contribute measurement methodology, convert co-members → intros.
- **ISO/IEC 42001/42005 + EU harmonised standards** — the estate's governance stack maps here;
  position the measured gauge as the conformity-assurance evidence.

### 6. Anti-commoditization (the "cheating to win" problem)
The research confirms the industry games benchmarks. Our **contamination register + signed runs +
designed-resistant probes** is the direct answer. Make it the differentiated, licensable asset
(the data licensing moat, [GATE] on consent but the register is [in-lane] done).

## What's genuinely missed (honest)

- **Fusion** — the four assets aren't connected (the product doesn't exist yet).
- **External/** positioning — everything is internal; nothing is published as the compliance asset.
- **Trust flip** — still gated on a quiet GPU.
- **Signing rail** — [GATE #dsh] (the "signed" part of the moat).
- **Consent + licensing** — the data product needs disclosed-participant consent [GATE].

## The next-week sequence (Mon → Sun)

1. **Mon**: Fuse the gauge+index+arena+contamination into `feeds/measured_compliance.json` — the
   single public evidence surface ([in-lane]).
2. **Tue**: Map the EU AI Act GPAI obligations → the measured evidence pack (article mapping).
3. **Wed**: Finish the trust flip (get a quiet GPU slot; run token-entropy; flip +7).
4. **Thu**: IP posture — OIN scope check + register the measured-gauge method.
5. **Fri**: Board/standards connections — draft the contributions for BSI ART/1 + OWASP AI-MCP.
6. **Sat**: Publish the anti-Goodhart contamination-awareness as a public explainer (the moat's
   differentiator).
7. **Sun**: Re-run E2E + scorecard; reconcile the NEXT-100 + this plan.

## Honest register
- [BUILT] gauge, arena Elo, contamination register, index, bridges.
- [BET] the fused "measured compliance" product is the win — *disconfirming*: nobody has licensed
  it yet; buyers need the EU AI Act enforcement to bite (it just started 2 Aug 2026).
- [GATE] signing rail, consent, trust flip (quiet GPU), IP filing (Nick).
