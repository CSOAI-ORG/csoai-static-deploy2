# PHYSICAL COMPUTATION MAP — the core-rope / water-memory / EUNOMIA vision, grounded
## 2026-08-21 · FRAMEWORKS DRUM · honest triage: real science · buildable-now · metaphor

> Nick's physical-computation thread (Apollo core-rope, water/capillary memory, Venturi,
> Riemann, fly brain, LEDs, "EUNOMIA") is **inspiration, not spec** — the source text is a
> brainstorming stream. This map grounds each idea against (a) what the estate ACTUALLY has
> on disk (verified paths), (b) published science, and (c) what is buildable with current
> tech TODAY. Nothing below claims hardware that does not exist.

## 1. What the estate actually has (verified on disk, 2026-08-21)

| Idea in the vision | Real estate artifact | What it actually is | Honest status |
|---|---|---|---|
| "SOVOS visual mind" | `clawd/kimi-regen/soul/visual_mind.py` + `csoai-static-deploy2/soul/` | **VLM registry** — MiniCPM-o 4.5 / InternVL3.5 / Qwen2.5-VL etc.; "honey fluid mode" = frozen knowledge base + VLM visual understanding on top | CODE EXISTS (config/registry + harness); needs a VLM runtime to fire |
| "calilarty water memory" | `_TABS/_inventory/MEOK_SILICA_CAPILLARY_W12` (`00_SILICA_CAPILLARY_SYNTHESIS.md`) + `PROJECT_AURUM_W16_CAPILLARY_HUMANOID` + `whitepaper_capillary_cooling.md` | **5D silica optical memory** (femtosecond nanogratings; Southampton 2013 / Microsoft Project HSD real science — 360 TB/disc, 13.8B-yr stability) merged with **capillary microfluidic cooling** (lab-on-a-chip) — Project AURUM orb memory redesign | DESIGN/SPEC shipped (the doc, not the hardware) — real physics, R&D roadmap |
| "oscillator / clock" | the DRUM heartbeat clock (master doc §6: L0 1 Hz firefly, phase-veto) | A real software clock the whole estate already ticks on | RUNNING |
| "3KB honey" | the 3KB signed h3k card format | Real: 3KB ed25519-signed Q&A capsules, content-addressed | RUNNING |
| "water→milk→honey graded states" | conformal router nonconformity scores (this session) | Graded confidence → frozen threshold, `Pr[auto AND wrong] ≤ α` | RUNNING (scores; trust pending ensemble) |
| "GSPC is the Venturi" | GSPC 14-slot axis canon / the 16-axis sim axes | The axes ARE the selection bottleneck — the constriction that turns input into one measured vector | DESIGN (partial) |
| "mergekit physical" | geometry-core (Procrustes/Fisher-Rao) + the evolve loop | Merging, governance, memory federation = same math (already the master doc §5 note) | RUNNING (math core) |
| "neuro-symbolic OOWM" | OOWM = designed world-model (Mamba-2+MoE+attention, 16-dim state) — per honest register: STAGED, no weights | The estate's REAL neuro-symbolic pattern is elsewhere: **neural scores + symbolic frozen predicates** — the conformal router is the working instance | PARTIAL (router; OOWM staged) |

## 2. The honest neuro-symbolic answer

**Is the visual mind / OOWM a neuro-symbolic model?** Not in the "weights that reason with symbols" sense —
the visual mind is a VLM registry (pure neural vision), and the OOWM world-model is **staged**
(no weights exist, per the estate inventory). **But the estate already implements a genuine
neuro-symbolic architecture** — the deterministic-predicate doctrine:
- **Neural side:** small-model ensemble scores (this session), VLM visual perception, benchmark
  models.
- **Symbolic side:** frozen conformal thresholds, the 14-slot axis registry, the charter/framework
  predicates, the 3KB signed card schema.
- **The binding:** `s(x) ≤ q̂` — a neural uncertainty score gated by a symbolic, frozen,
  distribution-free guarantee. That IS neuro-symbolic computation with current tech, and it is
  what the estate can honestly claim (measured, not certified).

## 3. The vision → current-tech triage

| Vision idea | Published science (real) | Buildable NOW (software) | Buildable soon (hardware R&D) | Metaphor only |
|---|---|---|---|---|
| Core-rope through/around = bit | Apollo ROM; memristor/crossbar arrays (HP, IBM, NVIDIA research) | Crossbar-like associative ops = attention/matrix math (the estate's geometry-core already notes Procrustes underpins mergekit) | Memristor crossbar test chips | — |
| Water/capillary memory | FLODAC fluidic logic (1964, real); 5D optical storage (Southampton/MSR, real); microfluidics lab-on-a-chip (real) | Simulate fluidic states as graded/ternary logic (softmax already does this) | Silica-capillary orb prototype (the AURUM roadmap) | "water memory as a mind" |
| Venturi pressure/speed coupling | Bernoulli/fluid dynamics (textbook) | The "fast thinking = low pressure, high velocity" = the GSPC attention bottleneck — already the design metaphor | — | The universal "differential engine" framing |
| Riemann critical line ↔ order/chaos | Zeta zeros + Hilbert-Pólya (real math; Anthropic's 67.2% bound is published) | — | — | "beliefs as zeros on a line" is evocative, not an architecture |
| Fly brain | Drosophila connectome (real, published); SpiNNaker/neuromorphic SNNs (real) | 8-bit sparse weights, massive dim-reduction (real ML practice) | Neuromorphic boards (Loihi/TrueNorth class) | — |
| LED/photonic output | LED physics (real); photonic interferometry (real research) | **The estate's real version: visual pattern detection over the signed-card corpus** (master doc §4 — the third signal layer) | Photonic compute (research) | "an AI you can photograph thinking" |
| "EUNOMIA = governance of interference" | — | The estate's real equivalent: **composed-never-fused (Dorado doctrine) + decorrelated voting (master doc §6) + the DRUM clock** — order from interference, already doctrine | — | The cosmic framing |

## 4. What this means for the build queue (no hardware claims)

1. **The neuro-symbolic claim that is REAL and measured:** the conformal router (neural score +
   symbolic frozen threshold). The trust flip (P12) completes it — that is the deliverable.
2. **The visual-mind lever:** wire `soul/visual_mind.py`'s VLM registry into the visual-pattern
   signal layer (master doc §4) once a VLM runtime is available — the "honey fluid mode"
   (frozen knowledge + visual) is exactly the estate's retrieval-grounded pattern.
3. **The AURUM silica-capillary roadmap stays a roadmap** — real physics, farm-manufacturable
   in principle, but hardware R&D with no dated commitment. The drum records it honestly as
   DESIGN (Project AURUM cards exist in the estate).
4. **The oscillator already exists** — the DRUM clock. Any "new" physical-clock idea maps to
   the existing heartbeat; no new component required.
5. **Graded states already exist** — the conformal score is the water→milk→honey continuum:
   low score = water (auto), mid = milk (escalate), high = honey (blocked/review). The
   three-way texture of belief is already implemented as the 90/10/escalate routing.

## 5. Rule (binding)

- The drum indexes the REAL artifacts (visual_mind.py, silica-capillary synthesis, AURUM
  cards) as estate items; it does NOT index the vision text as fact.
- Any future claim about "physical/hardware memory" must carry the honest register:
  `DESIGN (physics real, hardware not built)` — never `RUNNING`.
- The EUNOMIA frame is usable as internal shorthand for "governance of interference" and maps
  to existing doctrine (composed-never-fused, decorrelated voting, the DRUM clock); it adds no
  new capability claims.
