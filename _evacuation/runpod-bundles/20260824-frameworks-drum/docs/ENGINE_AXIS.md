# ENGINE AXIS — the one measurement engine, all domains

> Doctrine-clean answer to "can we bridge banks · insurance · AI · COBOL · bonds · stocks ·
> East↔West into one engine axis that measures, signs, and governs with safety nobody else
> has?" Every claim tagged [BET]/[BUILT]/[GAP]. Internal codenames stay quarantined.

## The one engine (built once, applied everywhere)

There are not 15 engines. There is **one**:

```
monitor → analyze → plan → execute → (knowledge)   [MAPE-K, collapsed from the 9-step loop]
        └── score → conformal-gate (90/10) → route ── [split-conformal; Pr[auto AND wrong] ≤ α]
        └── sign → content-address → chain ────────── [Ed25519 rail; signed cards]
```

Every domain — insurance, governance, bonds, gaming, defence, space — is an **instance** of this
loop with a different **domain corpus** (the drum's index) and a different **measured gauge**
(the trust gauge). The machinery is domain-agnostic. [BUILT for the loop + conformal router;
[GATE] for the signing rail]

## The diagonal (the actual monopoly)

| Axis | Competing on it | Already owned by |
|---|---|---|
| X = speed (T+0, stream) | the "pipe" | MarketAxess, OpenYield, exchanges |
| Y = trust (audit, legal) | the "vault" | DTCC, law firms, regulators |
| **the diagonal** | **speed AND trust at once** | **the signed-measurement layer — nobody** |

The diagonal is the only path that is *simultaneously* fast and auditable, because the
verification is computed and signed in the loop rather than bolted on afterward. That is the
moat. [BET — the counter-evidence is that the signing rail is not live, so the diagonal is
currently a *design*, not a product; what survives: the conformal router + reference index are
real and are the necessary substrate]

## The domain map (the "15 axis")

The engine is one; the **corpus** (what the loop measures) is per-domain. The drum is the
reference index that holds every domain's instruments:

- **Frameworks** (219) — NIST, ISO/IEC, COSO, Basel, MiFID II, DORA… the *governance* layer.
- **Regulations** (126) — the *legal* layer (EU AI Act, GDPR, Dodd-Frank, EMIR, HIPAA…).
- **Benchmarks** (36) — the *measurement* layer (MMLU, SWE-bench, GPQA, AILuminate…).
- **Charters** (136) — the *principles* layer.
- **Sectors** — the *market* layer (expanding to bonds, insurance, equities, banking/COBOL).

Each domain's flywheel is the same: mine → index → measure → attest → (consented) data. The
blockers the loop removes are the **friction in the middle** — the opacity, the un-signed
provenance, the manual compliance — not the market itself.

## Two heads, one engine

- **CSOAI = the body** — the verification/measurement layer. [BUILT: drum 661 items, conformal
  router, signed-card format; [GAP]: the live Ed25519 rail]
- **MEOK = the public face** — the consented gaming/eval end. [BET — every probe *disclosed and
  licensed*; the "unknowing gamer" harvesting model is a red line and is rejected here]

One engine underneath both. The body certifies nothing; it attests a measurement. The face
generates consented data; it never harvests unaware users.

## What is NOT built autonomously (Phase 2, compliance-gated)

Smart contracts, tokens, staking, agent wallets, slashing, atomic DvP, compute futures,
prediction markets — **all crypto/money layers**. They require Nick's go + legal/securities
opinion + audit. See `ops/EUNOMIA_PHASED_PLAN_2026-08-23.md`. The engine axis is the
*measurement* layer under Phase 0; it does not reach into Phase 2.

## The honest register

- [BUILT] MAPE-K loop, conformal 90/10 router, reference index (661 items), signed-card format,
  CI cross-check, board.
- [BET] the diagonal monopoly, the data flywheel, MEOK-as-consented-eval.
- [GAP] the live Ed25519 signing rail, live gauge scoring, any money/crypto layer.
- Rejected: "unknowing gamer" harvesting, self-graded revenue projections, certification claims.
