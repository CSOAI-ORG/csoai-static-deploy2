# CORRECTIONS LEDGER — FRAMEWORKS DRUM
## Append-only record of every correction this lane made (P19-89) · starts 2026-08-20

> Rule (master doc §8.4): corrections are logged, supersede never delete, and every entry
> names the correction, the evidence, and who caught it. This ledger doubles as the
> anti-Goodhart audit trail for the drum's own claims.

| # | Date | Correction | Evidence / caught by |
|---|---|---|---|
| 1 | 2026-08-20 | **did:web split-brain direction corrected.** Earlier live check showed the mirror LACKING board-attestation-1; a re-check after the mirror updated showed it carrying the EXTRA card-attestation-1 (apex 3 keys, mirror 4). WIRING §0 rewritten to the live state; the one-PR reconcile (add card-attestation-1 + #dsh to apex) stands. | live curl of both endpoints, 2026-08-20 |
| 2 | 2026-08-20 | **E2E protocol bug caught.** The first E2E conversation sent `"method": "<tool>"` instead of `"method": "tools/call"` — the server was correct, the test was wrong. Fixed in `tests/e2e_drum.py`. | e2e suite's own first run |
| 3 | 2026-08-20 | **Conformal-router selftest toy model was invalid.** The first selftest labelled 'correct' with a fixed threshold unrelated to the score — the conformal guarantee did not apply. Rewritten to verify the actual guarantees (quantile property + realized bound under a score-correlated error model). | router selftest failure |
| 4 | 2026-08-20 | **Lint false positives on internal-flagged items.** The first lint scanned raw catalog.json text and flagged internal-codename items that are *by design* stored flagged. Lint now parses catalog.json and checks only the public subset. | lint gate run |
| 5 | 2026-08-20 | **BFT codename leaked via a status field.** `charter-of-charters.status` carried "BFT #1". The scrub loop now covers status/issuer/region/effective, not just description. | lint gate run |
| 6 | 2026-08-20 | **Duplicate id `eu-ai-pact`.** Seed and web-mined items slugged identically. Builder now enforces unique ids (suffix on collision). | test suite (dup-ids check) |
| 7 | 2026-08-20 | **6 stale cards** from pre-dedupe builds. `write_cards` now prunes cards not in the catalog. | test suite (cards==items) |
| 8 | 2026-08-20 | **Regulations web sweep settled without its file.** Recovery turn interrupted after two failed landings; its unique substance (2025-26 instruments) was already folded via the estate 236-list sweep. Registered as a known gap (scorecard −4) rather than silently substituted. | tray scan + agent status |
| 9 | 2026-08-20 | **"150 items seed" registry note went stale.** The registry tile note froze at the first build; corrected to 559/581 with feeds + v1.4 (top-down alignment pass). | alignment sweep |
| 10 | 2026-08-21 | **Measured labels are PROXY, not real scores.** 80 labels collected from arena rounds carry no pre-label nonconformity feature; `drum_route` reports `trusted: false` and will not claim otherwise. The trust flip needs the ensemble-disagreement score (P12). | collect_measured honest register |
| 20 | 2026-08-22 | **Recurrence of the anchor-consumption bug (#18) — this time it broke the iso-10668 seed entry AND the rebuild failure was masked.** The seed-edit `replace(anchor, add)` (add without anchor) consumed the iso-10668 opener again; the `build_catalog.py --check --lint >/dev/null` piped the SyntaxError away, so the catalog stayed stale (612, no 8477) and a broken file was committed. Fixed by re-injecting the opener; catalog now 613 (8477 + 10668 both present). LESSONS: (1) `replace(anchor, add)` must be `add + anchor` — never drop the anchor; (2) NEVER pipe build_catalog.py to /dev/null — always check its exit/status; (3) a seed-integrity guard added to tests/test_drum.py (every SEED entry has id+name) so a dangling entry fails the suite, not just the rebuild. | seed inspection + rebuild exit check |
| 19 | 2026-08-22 | **Regulations web-sweep retirement decision (P23-25).** The 2026-08-20 web regulations sweep settled without its file; after two recovery attempts its unique substance (2025-26 instruments incl. EU Digital Omnibus, US state wave, HIPAA NPRM) was confirmed already folded via the estate 236-list sweep. DECISION: retire the recovery as blocked-by-coverage (scorecard keeps the honest -4 until any genuinely-new instrument appears); a fresh full web regs sweep is a [LANE]/future task, not a retry of the dead one. Recorded rather than silently dropped. | tray + scorecard inspection |
| 18 | 2026-08-22 | **Seed-edit bug deleted the councilof-ai-monorepo entry opening.** A `str.replace(anchor, add)` used `add` without re-including the anchor, consuming the entry's opener and leaving a dangling `"name":` — SyntaxError caught by the rebuild (never committed broken). Fixed by re-injecting the opener. Lesson: `replace(anchor, add)` must be `replace(anchor, add + anchor)` or the anchor is lost. | rebuild failure + ast.parse |
 **Label-leak in the corpus-model scaffold (caught by its own protocol).** First train scored MLP test accuracy 1.000 vs baseline 0.317 — meaningless: the kind one-hot IS the kind label, the model copied it back. Fixed: task-specific feature exclusion drops the kind one-hot; the promote-if-better verdict only counts on the de-leaked features. The 1.0 was never reported as a capability — it was logged as a leak. | corpus_model run + report inspection |
| 16 | 2026-08-22 | **Scorecard trust inflation (same class as #12, in the scorecard).** `ops/scorecard.py` granted Stage-1 +7 (20/20, 'trusted=YES') from proxy-status alone while the coverage marker said trusted:false — the scorecard over-claimed 93.7. Fixed: the scorecard now reads `feeds/router_trust.json` (same authority as drum_route); honest total is 87.0 with stage-1=13, trusted=NO. ALSO fixed the ops dimension scoring 0 (the full verify-only exceeded the 120s sh timeout — raised to 300s). The alignment audit caught the drift by design. | alignment audit + scorecard inspection |
| 15 | 2026-08-21 | **Same supersede flaw in the ensemble path (drop-before-score).** The local-ensemble scorer dropped ALL proxies for the 79 target findings first, then scored — the 32 that failed to score (model no-verdict) lost their proxy entries (measured 80 -> 48). Fixed: drop a proxy ONLY after a real score for that finding is appended (drop-after-success); skipped findings keep their proxies. Ledger #14's fix was the frontier path; this is the ensemble path. The negative coverage result is robust across both runs (0.53, 0.37). | count inspection after re-score |
| 14 | 2026-08-21 | **Calibration-set wipe incident (root cause: supersede-without-safety-net + Gemini quota 429).** The frontier re-score dropped all 79 prior labels for the target findings, then Gemini quota exhaustion (429) made every call fail -> 0 scored -> set nearly wiped (measured 80 -> 1). RECOVERY: the labels are regenerable from the immutable source (signed_rounds.jsonl); collect_measured + ensemble re-score restore them. FIX: frontier_score.py now backs up SET_PATH before superseding and restores on scored==0 (fail-closed, never wipe). ALSO: the Gemini key was hardcoded in the scorer — the keystone guard blocked the commit; key now read from env/litellm config at runtime, never committed. | keystone guard + calibration-set inspection |
| 13 | 2026-08-21 | **Hedging/length text scores: no signal.** Two deterministic pre-label score attempts on the 79 labeled rounds failed: (a) hedging lexicon — the AI answers are terse verdicts ('YES', all ≤8 chars), zero hedge hits; (b) answer-length — no correlation. Second negative result for the trust flip; recorded so nobody retries blind. Frontier-model confidence (Gemini via the estate litellm key) is attempt #3, running. | scoring experiments |
| 12 | 2026-08-21 | **drum_route would have claimed trusted without the coverage marker.** After the realized-coverage check failed (realized error 0.53 vs α 0.05 — the 3-model ensemble disagreement does NOT predict the human-reference outcome on these probes), `drum_route` still reported `trusted: true` because it inferred trust from proxy-status alone. Fixed: the tool now reads `feeds/router_trust.json` — the marker is the ONLY trust authority; absent/failed => trusted:false with the reason. **The P12 trust flip is NOT achieved; the negative result is published in the marker (this is the doctrine working).** | coverage check run + tool audit |
| 11 | 2026-08-21 | **E2E recursion incident.** Adding `e2e_drum.py` to the standing check while `e2e_drum.py` ran the standing check caused mutual recursion — the timed-out loop appended 533 duplicate fold entries to the Knowledge archive before the kill. Fixed: e2e's last step is now a direct read-only check+lint (no script call); the fold-hook dedupes by fold date; the archive was cleaned 538→5 with duplicates removed and this entry logged. | e2e timeout + archive count inspection |
| 21 | 2026-08-22 | **Archive regrowth from test harnesses (root cause: append() not idempotent + tests wrote to production).** `knowledge_archive.selftest()` (candidate-A/B) and `e2e_properties.p_archive_invariant` (invariant-check) appended to the PRODUCTION `archive/store/index.jsonl` on every run — 881 test-pollution entries vs 3 genuine `drum-fold` entries. Fixed at the choke point: `append()` is now content-addressed-idempotent (same content_id → no-op) and resolves its index path from `KNOWLEDGE_ARCHIVE_INDEX` at call time; `selftest()` and the property test isolate into a temp store. Cleaned 884→3 (the 3 genuine fold entries). | archive count inspection (884 lines / 6 distinct ids) |
| 22 | 2026-08-22 | **Mining-parser field-alias collision corrupting status/binding.** `FIELD_ALIASES` mapped `"effective"` and `"binding"` into the `status` field, so estate.md's `**Status:** LIVE | **Binding:** yes` parsed to `status="yes", binding=null` (257 items). Also broke the reverse-mine `**Effective:**` label. Fixed: `"status": ["status"]` only; `binding`/`effective` map to their own fields. Result: 64 estate regulations now honest (`LIVE`/`PROPOSED` + `binding:true/false`) instead of `yes/no` + `null`; seed (62 regs) untouched. | catalog diff (506 status + 506 binding lines, nothing else) |
| 23 | 2026-08-23 | **Three board/register/ops drifts caught in the alignment rundown.** (a) **Site nav 404:** `build_drum_site.py`'s overview nav used `/{k}.html` (singular) while every kind page uses `PAGES[k]` (plural) — the live board's home page linked to `framework.html`/`charter.html`/… which don't exist, so all overview nav was 404. Fixed: overview now uses `PAGES[k]`. (b) **EAT 7-box "boarded" + WIRING under-claimed:** both said "no public board page" / "not boarded" while the board IS live at frameworks-drum.pages.dev (HTTP 200). Fixed: `write_feeds` marks boarded=True with the live URL and now COMPUTES the 7-box status line (3/7 true, 2 partial, 2 false); WIRING §7 updated. (c) **Midnight scorecard-freshness fragility:** `align_audit` failed "scorecard mentions today" right after date rollover, so the 15-min + overnight agents false-alarmed every cycle until a scorecard.py run; scorecard.py's own ops gate transiently dropped to 81.8 (stale scorecard → align_audit fail → ops=0). Fixed: align_audit accepts today-or-yesterday (24h grace), and the overnight now refreshes the scorecard after rebuild. | LaunchAgent logs (FAIL: top-down alignment audit @02:54/03:09) + curl 200 + nav href scan |
| 24 | 2026-08-23 | **Lint blind spot on the live board.** `lint_surfaces()` scanned `llms.txt`/README/WIRING/manifest/agent-card but NOT the deployed `site/*.html` + `drum.llm.json` — so an internal codename or language lock could have reached frameworks-drum.pages.dev unnoticed if `build_drum_site.py`'s scrub ever regressed (the site was clean only by the builder's own scrub, not by the gate). Fixed: the lint now scans all `site/*.html` + `drum.llm.json` for codenames AND language locks. Verified by injecting `sov3` into a temp site file → caught. | injected-leak test (`site/_TEST_LEAK.html: sov3` caught) |
| 25 | 2026-08-23 | **Holy-of-sources citation corrections (two flagged items verified).** (a) **ISO 20077 had the WRONG title** — recorded as "Vehicle CAD (Computer Aided Diagnosis)"; the actual series is "Road vehicles — Extended Vehicle (ExVe) methodology" (Part 1 general, Part 2 design methodology, TS 20077-3:2024 upstream process). Title corrected, status → active (multi-part series). (b) **NIST IR 8477 withdrawn-draft confirmed** — `NIST.IR.8477.ipd.pdf` is explicitly marked "Withdrawn Draft"; the IPD was never finalized. Status changed from "verify final status" to "withdrawn draft (IPD never finalized)" and the estate source now points at the withdrawn PDF. | web verification (AFNOR/DIN for 20077 parts; nvlpubs.nist.gov for 8477 Withdrawn Draft) |
| 26 | 2026-08-23 | **14 phantom section/topic headers folded as items.** `parse_mining_file` treated `## SECTION N`, `## Topic N`, and `## Cross-cutting notes` dividers as real items — 14 empty-description phantom "articles" (no region/sources/estate) polluted the catalog. Fixed: the parser now skips structural dividers (regex `section N`/`topic N`/`cross-cutting`/`appendix`/`part N`). Catalog 639 → 625 (219 frameworks / 136 charters / 126 regulations / 139 articles / 5 sectors); 14 stale cards pruned; zero real items lost (mined counts: frameworks 48→47, articles 60→54, estate 260→253 — exactly the 14 dividers). | data-quality scan (14 empty-description items, all `SECTION`/`Topic`/`Cross-cutting` headers) |
| 27 | 2026-08-23 | **5 near-duplicate instruments folded twice (root cause: exact-name dedup missed name-variants).** (a) **NIST AI RMF** was indexed 4× — once as a *charter* (wrong kind) and 3× as framework variants (base "NIST AI RMF 1.0", "…(AI RMF)", "…(AI RMF) 1.0 — NIST AI 100-1") plus the GenAI Profile as both seed `nist-ai-600-1` and a mined "RMF Generative AI Profile"; (b) **ASEAN Guide** appeared as both "…& Ethics" (seed) and "…and Ethics" (mined); (c) **Australia AI Ethics** as both "Framework" and "Principles". Fixed: `canonical_key()` collapses same-instrument variants (keep seed/framework, drop wrong-kind/redundant), and `norm()` maps "&"→"and" so ASEAN collides. Catalog 667 → 662 (frameworks 221→218, charters 136→134); 4 stale cards pruned. | near-dupe scan (6 groups; 3 were real) |
| 28 | 2026-08-23 | **6 stale web citations (all 404 — pages moved) + CI cross-check field gap.** Extending `ci_crosscheck.py` to scan BOTH the `estate` and `sources` fields (was estate-only, 40→135 citations) surfaced 6 genuinely-dead URLs: AWS Responsible AI, gov.uk Frontier-AI-Safety + Seoul Declaration ×2 (path changed), Salesforce AI-for-Humanity (site restructured), White-House AI-Bill-of-Rights (moved to the archives), ITU AI-for-Good (restructured). Each re-verified via web (holy-of-sources) and corrected to the live URL. Also: the classifier was refined so only definitive 404/410 are DEAD (bot-blocks 403/418/429/202 = alive; 000/5xx = transient, warn not gate-fail). Result: 135 citations checked, **0 DEAD** (was 6), gate green. | ci_crosscheck re-run (105→111 resolve; 6 DEAD → 0) |
| 29 | 2026-08-23 | **Archive was content-addressed but NOT tamper-evident (no hash-chain).** `content_id` was sha256 over (kind,name,payload) only — it did not commit to the previous entry, so editing/reordering an entry was undetectable. Fixed: `append()` now writes `prev_hash` + `chain_hash` (sha256 over content_id ∥ prev_chain_hash), making the append-only history a genuine tamper-evident hash-chain; the selftest verifies every link + the migration chained the 4 existing production entries. NOTE (doctrine): this is the LOCAL 'chained' foundation; the signed Ed25519 sigil-chain remains a [GATE]. | selftest (hash-chain verified) + migration (4 entries chained) |
| 30 | 2026-08-23 | **TEA walk had 3 blind spots (found by adversarial tampering).** (a) **Card-content drift:** the dual-walk only checked card file *presence*, not content — tampering a card's name went undetected (an exact-title check was needed; `startswith` missed an appended suffix). (b) **Hash-chain drift:** after ledger #29 the walk didn't verify `prev_hash`/`chain_hash` links, so a forged archive entry slipped through. (c) **Router-trust marker:** the docstring claimed to report it but the code never read `feeds/router_trust.json`. Fixed: `forward_state()` now exact-matches each card's title to the catalog item; `tea_walk()` verifies the hash-chain; `trust_marker()` reports the honest trust state without re-deciding it. | adversarial test (content-tamper → caught; fake-archive-entry → `chain_drift`; clean → `claim REAL`) |
