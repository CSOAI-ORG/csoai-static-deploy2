# ECOSYSTEM ALIGNMENT + SERIES-A FRAMEWORK (2026-08-24)

> Align every data-generation asset + product into ONE ecosystem, and position it for funding
> (Series A) with the moat, defensible IP, and revenue streams. Doctrine: measurement not
> certification; [BUILT]/[BET]/[GATE]; no fabricated numbers.

## The ecosystem (5 layers, one flywheel)

```
 5. REVENUE LAYER  — compliance-as-a-service · data licensing · model-routing · arena subscription
 4. PRODUCT LAYER  — 62 csoai-* MCP packages (arena, axis-engine, fisher-rao, crosswalk, council…)
 3. MEASUREMENT LAYER — the measured gauge (30K records, 18 axes) + arena Elo + conformal 90/10 router
 2. DATA LAYER     — contamination register · jailbreak/fleet datasets · eval-transcripts (consented) · bridges
 1. REFERENCE LAYER — the drum: 662 sourced instruments (frameworks/charters/regulations/articles/sectors/benchmarks)
```

The flywheel: reference layer (what exists) → data layer (the measured/jailbreak/contamination
corpus) → measurement layer (gauge + arena Elo + trusted router) → product layer (MCP packages) →
revenue layer (the Series-A streams). Each layer feeds the next; the whole thing is the
"measure and attest" engine applied across every domain.

## Data-generation assets (BUILT — the corpus)

| Asset | What | Layer |
|---|---|---|
| `board-live.json` | 30,252 live records, 97 signed cards, 18 axes | measurement |
| `arena-elo.json` | Bradley-Terry on own GSPC pillars | measurement |
| `benchmark_contamination.json` | 36 benchmarks, 12 designed-resistant | data (anti-Goodhart) |
| `jail-*` (full/consolidated/live/final…) | adversarial/jailbreak fleet datasets | data |
| `big-bench-results`, `general-capability-probe`, `cross`, `domain-gateway` | evaluation runs | data |
| Bridges (bond router, insurance engine, data DAO, agent economy) | domain products | product |
| 62 `csoai-*` packages | the MCP/A2A product surface | product |

## Defensible IP (the moat)

1. **Contamination-aware measured-gauge method** — evaluate on own pillars + contamination
   register + designed-resistant probes + realized-coverage bound. OIN-clean (measurement, not
   Linux-adjacent → grant-back does NOT apply → ours).
2. **Split-conformal 90/10 trusted router** — `s(x) ≤ q̂`, `Pr[auto AND wrong] ≤ α`.
3. **Signed-card format + GSPC/Fisher-Rao axes**.
4. **The accumulated corpus** — UK/EU database right (asserted via IP_NOTICE).
5. **The reference index + crosswalk** — the sourced instrument map.

**IP growth via the "ASI evolve" discipline**: apply promote-and-keep-if-better to the assets — a
new designed-resistant probe set, a new contaminm register entry, a new axis — is evaluated,
and *promoted only if it beats baseline with statistical significance* (the mergekit/keep-if-better
loop at the knowledge layer). Every promoted artifact both improves the moat AND is a new data/IP
unit. This turns "more assets" into "a self-improving method."

## Revenue streams (Series-A positioning)

| Stream | Today | Series A | Moat |
|---|---|---|---|
| Compliance-as-a-service (EU AI Act GPAI evidence pack) | [BUILT] gauge+contamination+GPAI map | the pack | measured+contamination-aware — what the "cheating" industry can't produce |
| Data licensing (eval-transcripts, contamination register) | [BUILT] register | consented transcripts | [GATE] consent |
| Model-routing (OpenRouter-parity) | sibling-built router | routed + measured | conformal-trusted (needs trust flip) |
| Arena/benchmark subscription | [BUILT] arena Elo | measured leaderboard | own-pillars + anti-Goodhart |

## Series-A thesis (honest)

**"We own the measured, signed, contamination-aware evaluation layer the EU AI Act GPAI + the
anti-benchmark-gaming movement both demand — as defensible IP and a licensable data product."**
- TAM: EU AI Act compliance + model-evaluation market (multi-billion; enforcement started 2 Aug 2026).
- Moat: the method (OIN-clean IP) + the accumulated contamination-registry + signed measurement.
- [BET] buyers will pay — *disconfirming*: enforcement just started; nothing licensed yet; the
  window is the next 2 quarters. Revenue figures are NOT claimed here (no fabricated ARR).
- [GATE] signing rail (#dsh), consent, trust flip (quiet GPU), IP filing (Nick + scope check).

## Deliverable (this doc) + the executable moves
1. The ecosystem is now mapped (this doc) — the assets are one flywheel, not scattered.
2. Next: promote the measurement-layer assets via the ASI-evolve gate; wire the GPAI pack into a
   pricing/landing surface; finish the trust flip (a quiet GPU).
