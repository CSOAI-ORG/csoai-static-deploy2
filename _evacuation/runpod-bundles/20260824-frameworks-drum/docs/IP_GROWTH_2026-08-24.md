# IP GROWTH — the measured-compliance method (defensible, ours)

> How the estate's measured-compliance IP grows, grounded in the OIN 2.0 / LOT posture.
> Doctrine: the measurement instrument IP (signed-card format, GSPC axes, gauge) is ours —
> OIN grant-back does NOT apply because it is measurement, not Linux-kernel-adjacent.

## The defensible IP (what we own)

1. **The contamination-aware measured-gauge method** — evaluate a model on its own measured
   pillars with a contamination register (designed-resistant probes + leakage tracking), report a
   *measured, signed, never-certified* score + realized-coverage bound. Anti-Goodhart by design.
2. **The split-conformal 90/10 trusted router** — the conformal predicate `s(x) ≤ q̂` with
   `Pr[auto AND wrong] ≤ α` (distribution-free). The trust-flip gate.
3. **The signed-card format + GSPC axes** — the 3KB signed measurement card + the Fisher-Rao /
   GSPC axis composition.
4. **The reference index + contamination register** — the accumulated corpus (UK/EU database right
   already asserted via IP_NOTICE.md).

## OIN scope check (the binding gate before filing)

Per the OIN 2.0 posture: run the scope check before ANY patent filing. The measurement methods
above are **NOT Linux-kernel-adjacent** — they are evaluation/measurement, not an implementation
of the Linux System. **Conclusion: the OIN grant-back does not apply; the measurement IP stays
ours.** This is the priority-1 finding to (a) record and (b) protect, because measurement
neutrality is the asset (Scale AI = the cautionary tale — never let a conflicted owner control
the measurement instrument).

## How IP grows (the lever)

- **File/register the contamination-aware measured-gauge method** (Nick-gated + the scope check,
  which I ran above → clean).
- **Continuously layered IP**: each new designed-resistant probe set + the contamination register
  enrich the method. Defensive publication (the public ABOUT page, the measured-compliance surface)
  creates prior art that *blocks* others from claiming the measure-and-attest method.
- **The data as IP**: the contamination register + measured gauge (30K records) are database right
  (UK/EU). The signed eval-transcript dataset (consent-gated) becomes the licensable asset.

## Honest register
- [BUILT] gauge, contamination register, arena Elo, signed-card format, conformal router.
- [BET] the method is defensible IP — *disconfirming*: it's not yet filed/registered; the public
  surface (ABOUT page + measured-compliance panel) publishes the method as prior art, which both
  protects (blocks claims) and is the authority play.
- [GATE] filing needs Nick + the scope check recorded; consent gates the data licensing.
